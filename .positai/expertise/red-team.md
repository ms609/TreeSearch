# Red-Team Expertise — TreeSearch

## Purpose

Red-teaming reviews code for (i) bugs and (ii) performance issues.
Fix trivial issues directly; add non-trivial ones to `to-do.md`.

## Focused rotation system

Each S-RED invocation targets **one focus area** from the rotation below.
The agent reads `last_focus` (bottom of this file) to determine which area
was reviewed last, then picks the **next** area in sequence. After
completing the review, update `last_focus`.

### Focus areas

| # | Area | Scope | Key questions |
|---|------|-------|---------------|
| 1 | **Fitch scoring correctness** | `ts_fitch.h/.cpp`, `ts_fitch_na.h`, `ts_fitch_na_incr.h` | Does incremental scoring match full `score_tree()`? Bounded variants bail correctly? NA three-pass edge cases? Write a targeted test if you find a gap. |
| 2 | **Search topology invariants** | `ts_tbr.cpp`, `ts_drift.cpp`, `ts_search.cpp` | After every rejected move, is topology fully restored? Undo stack correct? No stale `postorder`? Symmetry-breaking hash collisions? |
| 3 | **Ratchet & perturbation** | `ts_ratchet.cpp`, `ts_sector.cpp`, `ts_fuse.cpp` | `active_mask`/`upweight_mask` fully restored after perturbation? Sectorial reinsertion reverts on worse score? Fuse exchange handles tied scores? |
| 4 | **Parallelism & RNG** | `ts_parallel.cpp`, `ts_rng.h/.cpp`, `ts_driven.cpp` | Thread-local RNG set before any search call? No R API calls from worker threads? Pool mutex correct? Atomic stop flag races? Seeds generated from R RNG before spawning? |
| 5 | **Data pipeline & simplification** | `ts_data.h/.cpp`, `ts_simplify.h/.cpp`, `ts_constraint.h/.cpp` | `build_dataset` handles edge cases (all-ambiguous, single-state, zero-weight)? `build_reduced_dataset` copies all fields? Constraint column-major indexing correct? |
| 6 | **R ↔ C++ interface** | `ts_rcpp.cpp`, `TreeSearch-init.c`, `R/RcppExports.R`, `R/MaximizeParsimony.R` | Arg counts match? Concavity sentinel translated correctly? Edge matrix conventions? Return value attributes set? Parameter validation in R layer? |
| 7 | **Shiny module wiring** | `inst/Parsimony/server.R`, `server/mod_*.R`, `server/events.R` | Forward-ref callbacks resolve? Cross-module `updateXxxInput` targets correct namespace? Reactive graph has no orphaned observers? `isolate()` used correctly in result observers? |
| 8 | **Test suite health** | `tests/testthat/test-ts-*.R`, `tests/testthat/helper-ts.R` | Tier guards correct? Any tests that always pass (vacuous)? Missing `TreeSearch:::` prefixes? Edge-case coverage gaps (3-tip, single-char, all-NA)? Flaky tests? |
| 9 | **Wagner & addition trees** | `ts_wagner.h/.cpp` | NA-incremental scoring staleness acceptable? Constraint mapping (LCA-based) correct? Retry loop fires when needed? 3-taxon base case handles all orderings? |
| 10 | **Profile & IW scoring** | `ts_fitch.cpp` (IW/profile paths), `ts_data.cpp` (precompute) | `e/(k+e)` delta correct? Profile `info_amounts` lookup matches? `concavity = 1.0` sentinel activates weighted path? `precompute_profile_delta` includes `precomputed_steps` offset? |

### Workflow for a focused review

1. Read `last_focus` below → pick next area in rotation.
2. **Read the target files** thoroughly (not just skimming). Understand the
   logic before looking for bugs.
3. **Construct a specific adversarial scenario** — e.g. "what happens if
   the ratchet upweights a character that's already at max weight?" — and
   trace the code path.
4. **Write or run a targeted test** that exercises the scenario. If the
   test passes, note it. If it fails, file a bug.
5. Check for any **recent commits** touching the focus files (`git log`).
6. **Build and run** at minimum the test files most relevant to the focus
   area (not necessarily the entire suite).
7. Update `last_focus`, report findings.

Total time budget: aim for depth over breadth. A focused review that finds
one real bug is worth more than a broad sweep that confirms "all green."

## Bug patterns (reference — from past rounds)

| Pattern | Where to check |
|---------|---------------|
| Missing `GetRNGstate()`/`PutRNGstate()` around `unif_rand()` | Any .cpp using randomness |
| `std::random_device{}()` ignoring `set.seed()` | Seeding of `std::mt19937` |
| GCC-only builtins (`__builtin_popcountll`, etc.) | All .cpp/.h files |
| `.inc` file changes not triggering recompilation | `ts_fitch_na.inc`, `ts_fitch_na_incr.inc` |
| Missing `TreeSearch:::` prefix in tests | `tests/testthat/test-ts-*.R` |
| Arg count mismatch in `TreeSearch-init.c` | After adding/removing Rcpp params |
| `R_PosInf` in Rcpp defaults | `R/RcppExports.R` after `compileAttributes()` |
| No revert on worsening move | Sectorial reinsertion, fuse exchange |
| `active_mask`/`upweight_mask` not cleaned up | Ratchet perturbation restore paths |

## Performance patterns (reference)

| Pattern | Where to check |
|---------|---------------|
| Unbounded indirect scoring (missing `_bounded` variant) | Search inner loops |
| Full `score_tree()` where incremental would suffice | After clip/regraft |
| `build_postorder()` called unnecessarily | After unclip or snapshot restore |
| Full-tree copy where save/restore suffices | Fuse, sectorial |
| Missing early termination in loops | Block iteration in scoring |

## Known fragile areas (reference)

1. `ts_rcpp.cpp` + `TreeSearch-init.c`: append-only, check arg counts.
2. `RcppExports.R/.cpp`: concavity `Inf` → `-1.0` sentinel after regen.
3. `.inc` files: `touch src/ts_fitch.cpp` after changes.
4. Parallel: `ts_rng.h` thread_local must be set before search.
5. `init_from_edge`: first child → left convention.

## Reporting format

```
| T-NNN | P1/P2 | OPEN | — | [Bug/Perf] Brief description | Found by S-RED focus #N. File:line. Details. |
```

---

## last_focus

area: 1
reviewed_by: Claude (cpp-search, 2026-05-26)
date: 2026-05-26
notes: Fitch scoring correctness re-restart — focused on T-300 NA dirty-set (014ccdea) + EW dirty-set (f531bbcd) + 3df90882 NNI IW fix correctness. **Files reviewed:** src/ts_fitch_na_dirty.h (full), src/ts_fitch_na_incr.h (cross-check of per-node logic), src/ts_fitch.cpp (fitch_dirty_downpass/uppass 267-370, extract_char_steps 691-755, score_tree dispatch 1006-1042, fitch_score_ew 975-1004, compute_weighted_score 957), src/ts_tbr.cpp (SPR accept dispatch 1138-1180, use_iw/has_na 586-594, apply_tbr_move 286-406), src/ts_search.cpp (NNI accept 75-129 with 3df90882 fix), src/ts_hsj.cpp (hsj_score 240-260), src/ts_driven.cpp (result.score assignments at 181/247/.../595). **Scenarios traced:** (a) NA dirty-set with deeply-nested start_a/start_b (LCA at root) — each dirty node appears exactly once in tree.postorder; no double-counting; postorder children-before-parents guarantees prelim/local_cost freshness on read. (b) Off-dirty standard-block `local_cost` after SPR — off-dirty means children unchanged → local_cost from prior full_rescore is still valid → Pass 3 sum correct. (c) Tip down2/subtree_actives after SPR — tips with changed parent (`ns`, `below`, `clip_node`) have new parent in `{nz, nx}` (dirty seeds); Step 4 of fitch_na_dirty_uppass visits them; tips with unchanged parent on dirty path also visited. (d) Reverse-postorder dirty_up propagation in Step 3 — parents visited before children; off-path siblings of dirty nodes are checked when their parent is dirty_up; `any_changed` propagates downward. (e) Pass 3 reads tip down2 — confirmed all tips needing fresh down2 have their parent dirty (and thus are processed in Step 4). **Findings:** (1) **T-300 NA DIRTY-SET CORRECT.** All five state arrays (prelim, final_, local_cost, down2, subtree_actives) consistent post-walk. EW: ew_total + ds.ew_offset; IW/PROFILE: extract_char_steps + compute_weighted_score (dispatches by scoring_mode). (2) **EW DIRTY-SET CORRECT** — visits each dirty node once in postorder; length_delta accumulated cleanly; sidesteps the 1e3fc9a7 overlap-chain bug by construction. (3) **3df90882 NNI IW FIX CORRECT** — `extract_char_steps` reads local_cost which is correct for whole tree after `fitch_incremental_downpass` (NNI only changes children at edge c; off-chain nodes retain valid local_cost from initial score_tree). Same pattern as T-300 SPR accept. (4) **NEW BUG FILED — T-306: HSJ/XFORM SPR/NNI accept-paths omit hierarchy DP contribution.** Both `tbr_search` SPR accept (ts_tbr.cpp:1146-1180) and `nni_search` accept (ts_search.cpp:79-95) update `best_score` as Fitch-only delta. For HSJ/XFORM modes (`use_iw = false` because concavity is HUGE_VAL), `best_score` drifts from true `score_tree` value because hsj_score/Sankoff DP is topology-dependent but never re-applied to `best_score`. Phase 1 candidate evaluation has always been Fitch-only — this is a pre-existing structural issue; T-300 changed which side of the asymmetry is wrong. User-visible scores correct (run_single_replicate recomputes via score_tree at 181/247/.../595). Search-quality regression only. Empirically verified on 15-tip HSJ dataset: user-visible score matches independent TreeLength computation (final recomputed externally). T-303 already tracks same family for sector path. **Trivial fixes applied: none** (T-306 fix is non-trivial — guard `is_spr && !has_na` and `is_spr && has_na` branches behind `EW/IW/PROFILE/XPIWE` mode check, or extend candidate evaluation to include hierarchy DP). **Notes for next reviewer of area 1:** (i) Verify T-304 regression test (when filed) also covers the HSJ/XFORM cases — the existing four-path EW/IW/NA/NA-IW won't catch T-306. (ii) Bounded variants (`fitch_indirect_length_bounded`, NA variants) all check `if (extra_steps >= cutoff) return` after each block, but the brief asks about "bounded variants bail correctly" — confirmed: each variant returns early once cumulative steps reach cutoff, no off-by-one. (iii) NA three-pass edge cases: all-NA pattern (`needs_step` masked by `l_act & r_act` → 0 steps) and all-applicable pattern (case_strip/case_force/case_children unreachable depending on data) both handled correctly. **Next area: 2** (Search topology invariants — but already done today by parallel reviewer; effective next is 3).

Previous area: 2
reviewed_by: Claude (cpp-search, 2026-05-26)
date: 2026-05-26
notes: Search topology invariants — re-review post c504ea87 drift fix, T-300 dirty-set wiring, 3df90882 NNI IW fix. **Files reviewed:** src/ts_drift.cpp (all 6 apply/restore sites at 585-594, 598-615, 619-630, 631-651, 656-694, 782/791), src/ts_tbr.cpp (StateSnapshot 192-248, main loop 695-1318, especially T-300 dirty-set 1138-1180 and reject path 1251-1268, fast_undo lifecycle 651/762/1109/1318), src/ts_search.cpp (NNI 60-148 with 3df90882 IW fix; simple spr_search 350-407), src/ts_tree.{h,cpp} (save_node_state, PreallocUndo, restore_prealloc_undo, nni_apply/undo), src/ts_fitch.cpp (fitch_dirty_downpass/uppass 267-370, fitch_incremental_downpass 134, fitch_uppass 110, extract_char_steps 691, compute_weighted_score 957, fitch_score_ew 975), src/ts_fitch_na_dirty.h. **Scenarios traced:** (1) accept→reject→accept SPR sequence — state_snap.save() runs at top of each pass (line 704) capturing post-accept dirty-set-updated arrays; subsequent reject restore at line 1131/1198/1232/1254 returns to that snapshot; correct. (2) Off-dirty node correctness via induction — proved local_cost/prelim invariant maintained across SPR accepts (off-dirty nodes have unchanged children → unchanged states). (3) fast_undo (PreallocUndo) lifecycle — cleared at line 762 per clip, save_node_state writes during incremental passes (including dirty_downpass/uppass which DO push to it via save_node_state), restore_prealloc_undo at line 1109 ALWAYS precedes accept's apply_tbr_move so any dirty-set saves from a prior accept are cleared at the next clip's line 762 fast_undo.clear() — no stale entries leak across clips. (4) All 6 drift_apply_tbr_move failure paths in ts_drift.cpp — first apply (585-594) has restore_topology, second apply (677-685, fixed by c504ea87) now also has it. **c504ea87 verified correct** — matches prior review recommendation exactly; one-line fix at line 681. (5) NNI 3df90882 IW fix verified — extract_char_steps reads tree.local_cost which is correct for whole tree after fitch_incremental_downpass (off-chain unchanged from score_tree at entry). DEBUG gate correctly removed (it was masking the bug). NNI reject path (line 120-128) calls nni_undo + re-runs fitch_incremental_downpass(c) to restore prelim/local_cost — clean. (6) NA EW path ew_offset (line 1176) verified — best_score includes ew_offset, ew_total + ew_offset matches. (7) is_spr classification (line 1146) `best_reroot_parent < 0 || == clip_node` matches apply_tbr_move's skip-reroot condition (line 311) exactly. (8) Geometry of SPR dirty paths — proved that nz→root ∪ nx→root covers all nodes whose children change, including `above` (regraft point's outer parent; `above`'s child changes from `below` to `nx`; `above` is parent of `nx` in new tree → on nx→root path). (9) Symmetry-breaking hash collisions (Phase 3A virtual_prelim 64-bit FNV) — negligible. (10) StateSnapshot completeness — captures prelim, final_, local_cost, down2, subtree_actives, postorder. All fields needed for accept-then-reject restoration. **Trivial fixes applied: none.** **Findings: no new bugs.** The dirty-set wiring at ts_tbr.cpp:1138-1180 is mathematically sound and the reject paths properly capture/restore. **Open notes for next reviewer of area 2:** (a) NNI accept path at ts_search.cpp:101 calls fitch_uppass() unconditionally — this is O(n) but only c→root chain's children had topology changes; could be replaced with a dirty-set uppass like the SPR path uses. Pure perf opportunity, not a correctness issue. (b) Drift saved_postorder staleness after accepted moves (noted in 2026-05-19 review) — still present, still harmless (line 713-715 rebuild). (c) fast_undo capacity grows on overflow (PreallocUndo::grow at ts_tree.h:110) but never shrinks — could waste memory after a single deep tree. Cosmetic. (d) T-300 has no enduring regression test (per area #8 2026-05-26 note); when one is added, exercise accept→reject→accept sequences specifically. **Next area: 3** (Ratchet & perturbation).

Previous area: 3
reviewed_by: Claude (cpp-search, 2026-05-26)
date: 2026-05-26
notes: Ratchet & perturbation, sectorial, fuse, prune-reinsert — re-review with T-303 just filed. **Files reviewed:** src/ts_ratchet.cpp (full — save_perturb_state, restore_perturb_state, perturb_zero/upweight/mixed, ratchet_search), src/ts_sector.cpp (build_reduced_dataset 400-490, search_sector 500-540, rss/xss/css 640-1000), src/ts_fuse.cpp (accept_equal + revert path 420-500), src/ts_prune_reinsert.cpp (T-275 guard 490-501, build_reduced_dataset 290-334), src/ts_data.h (FlatBlock/CharBlock structs), src/ts_fitch.cpp (score_tree dispatch 1006-1042), src/ts_driven.cpp (sector + ratchet composition 220-380). ts_hsj.cpp unchanged in window, out of scope. `git log --since=2026-05-19` shows NO commits to scope files since prior review; 44d929a8 (the brief's "recent commit") is from 2026-05-18. **Findings:** (1) T-303 SCOPE CONFIRMED — All three sector entry points (xss_search at line 980, rss_search at line 840, css_search delegates to TBR with the reduced ds via search_sector at line 645) reach `score_tree(rd.subtree, rd.data)` at lines 512, 729, 876. score_tree (ts_fitch.cpp:1006) dispatches by `rd.data.scoring_mode` → for HSJ calls `hsj_score(tree, ds)` which reads ds.hierarchy_blocks (NOT copied → empty); for XFORM uses `ds.sankoff_n_chars`/`ds.sankoff_*` (NOT copied → sankoff_n_chars=0 → degrades to fitch-only). PROFILE is fine (info_amounts + info_max_steps + precomputed_steps + min_steps all copied at ts_sector.cpp:431-444). driven_search invokes xss/rss/css unconditionally for any scoring mode (no guard at ts_driven.cpp:236,255,278,342,355,371). T-303 correctly identifies HSJ + XFORM as the affected modes; all three sector entry points need the guard (or build_reduced_dataset extension). (2) 44d929a8 FLAT_BLOCKS COPY CORRECT — FlatBlock.active_mask is populated once at build_dataset (ts_data.cpp:190) and only modified by ratchet perturbation (ts_ratchet.cpp:39, 61, 123). Ratchet ALWAYS restores both blocks[].active_mask AND flat_blocks[].active_mask before returning. Sector and ratchet do not compose (sector is step 3, ratchet step 4 in driven_search outer loop — sequential, never nested). Verbatim copy of flat_blocks is correct; no per-block rebuild needed. (3) XSS+RATCHET TIED-SCORE TRACE — no interleaving possible; even hypothetically restore_perturb_state restores the full set: blocks[].active_mask, blocks[].upweight_mask, flat_blocks[].active_mask, pattern_freq. CLEAN. (4) NO NEW RATCHET ACCEPTANCE PATHS bypass full rescore — single accept path at ts_ratchet.cpp:213-227 uses TBR's `search_result.best_score` (internally via score_tree). (5) FUSE TIED-SCORE re-confirmed clean (ts_fuse.cpp:445-459 — topology-change check on accept_equal). (6) PRUNE-REINSERT T-275 guard intact at ts_prune_reinsert.cpp:497-501; this version still guards PROFILE additionally because prune-reinsert's build_reduced_dataset does NOT copy info_amounts/info_max_steps (asymmetric to sector). **No new bugs found.** **Open notes for next reviewer of area 3:** T-303 fix design choice: prefer extending sector's build_reduced_dataset to copy HSJ/XFORM fields over a guard (sector heuristic value worth preserving for these modes); but tip_labels/hierarchy_blocks need careful sector-subset semantics (non-trivial). The two `build_reduced_dataset` functions (ts_sector.cpp + ts_prune_reinsert.cpp) are an asymmetric latent footgun. FlatBlock.active_mask is duplicate-but-must-stay-in-sync of CharBlock.active_mask — three perturb modes already handle it; any future writer to blocks[].active_mask MUST add the sync. **Next area: 4** (Parallelism & RNG).

Previous area: 4
reviewed_by: Claude (cpp-search, 2026-05-26)
date: 2026-05-26
notes: Parallelism & RNG — re-review focusing on 2b6b6be7 (isatty replacement for R_Interactive) and b186e801 (R_FlushConsole guard). **Files reviewed:** src/ts_parallel.cpp (lines 1-50, 127-215, 280-468, 560-639), src/ts_rng.h/.cpp (no changes since prior review), src/ts_pool.cpp/.h (no changes), src/ts_driven.cpp (no parallel-relevant changes since prior review). `git log --since=2026-05-19` shows ONLY commit 2b6b6be7 touched scope files. **Findings:** (1) ISATTY MACRO PORTABILITY CLEAN — `#ifdef _WIN32` selects `_isatty(_fileno(stdout))`, otherwise `isatty(fileno(stdout))`. Both are standard C library APIs (MSVC <io.h>; POSIX <unistd.h>). `!= 0` comparison correct. (2) TTY CHECK CONFINED TO MAIN THREAD — All TS_ISATTY() / R_FlushConsole() / Rprintf() / R_CheckUserInterrupt() calls are in the main polling loop (lines 322-468). `worker_thread` (127-215) has zero R API calls or TS_ISATTY calls. parallel_resample worker (593-617) likewise clean. CLEAN. (3) STDOUT-REDIRECTED-STDERR-TTY CASE HANDLED — TS_ISATTY only checks stdout (the pipe the deadlock affects). If user redirects stdout to a file (`R -e '...' > log.txt`), `isatty(fileno(stdout))=0` → skip flush → safe. CLEAN. (4) RSCRIPT FROM TERMINAL — `R_Interactive=FALSE` (old guard suppressed progress) but `isatty(fileno(stdout))=TRUE` from a real terminal → new code SHOWS progress line. IMPROVEMENT, not regression. (5) UX REGRESSION — RSTUDIO PROGRESS NOW SUPPRESSED — In RStudio, R's stdout is a captured pipe (RStudio reads it); `R_Interactive=TRUE` but `isatty(fileno(stdout))=FALSE`. Old code (b186e801): showed `\r` overwrite progress at verbosity=1 in RStudio. New code (2b6b6be7): at verbosity=1, NO progress line displayed; at verbosity>=2, plain `\n` lines only. Not a correctness bug — `Rprintf` reaches RStudio via WriteConsole callback regardless — but interactive RStudio users at default verbosity will see no progress until search completes. Severity: LOW UX regression. Mitigation: either (a) accept it (RStudio users set verbosity=2); (b) extend the check — `TS_ISATTY() || (getenv("RSTUDIO") != nullptr)` — to detect RStudio specifically; (c) gate the `\r`+flush vs `\n` distinction on isatty but always EMIT something at verbosity=1. NEEDS FILING as non-trivial (requires manual testing in RStudio + R CMD check + terminal Rscript). (6) PRIOR FINDINGS ALL STILL CLEAN — seeds pre-generated with Get/PutRNGstate before spawn (lines 252-258, 583-587); deterministic per-replicate seed read at worker_thread:152; stop_flag `std::atomic<bool>` with relaxed ordering directionally safe; pool mutex held briefly for `best_score()`/`hits_to_best()` calls in perturb-stop block (no main-thread starvation); fuse_round long-mutex-hold remains performance concern only; consensus-stability check guarded by `done_now > last_stab_done` (line 383). (7) PERTURB-STOP DYNAMIC-LIMIT COMPUTATION DOES NOT HOLD POOL MUTEX — calls acquire/release internally; arithmetic at lines 415-420 uses local copies. Workers can make progress. CLEAN. (8) ts_rng.h/.cpp and ts_pool.cpp UNCHANGED since 2026-04 — prior E and 2026-05-19 reviews still authoritative. **One open issue identified (RStudio UX regression). No correctness bugs found.** Next area: 5 (Data pipeline & simplification).

Previous area: 10
reviewed_by: Claude (cpp-search, 2026-05-26)
date: 2026-05-26
notes: Profile & IW scoring — re-review post T-300 NA dirty-set + 3df90882 NNI IW fix. **Files reviewed:** src/ts_fitch.cpp (IW/profile paths, compute_iw, compute_profile, precompute_iw_delta, precompute_profile_delta, compute_weighted_score, extract_char_steps), src/ts_fitch_na_dirty.h (new file), src/ts_fitch_na_incr.h (fitch_na_pass3_score), src/ts_tbr.cpp (lines 1138-1180, NA dirty-set SPR accept wiring), src/ts_search.cpp (3df90882 NNI IW fix). **Findings:** (1) 3df90882 NNI IW FIX CORRECT — gated by `std::isfinite(ds.concavity)`, uses `extract_char_steps + compute_weighted_score` (dispatches IW vs profile by `ds.scoring_mode`). Same pattern as T-300 SPR accept path. After `fitch_incremental_downpass`, off-chain nodes retain valid `local_cost` from initial `score_tree`; chain walk updates on-chain nodes. `extract_char_steps` reads `local_cost` for non-NA blocks — correct. NNI for NA datasets routes through `score_tree()` per area #2 review (not through this incremental path), so NA interaction is not at issue here. (2) T-300 NA DIRTY-SET (014ccdea) IW PATH CORRECT — `fitch_na_dirty_downpass` updates prelim/local_cost/subtree_actives for nodes on the union of paths nz→root, nx→root. `fitch_na_dirty_uppass` updates final_ for those nodes plus immediate non-dirty children (via dirty_up propagation; off-dirty siblings of dirty nodes are visited because their parent is dirty). `fitch_na_pass3_score` runs full postorder, populating step counts from `local_cost` for standard blocks (correct: off-dirty nodes' `local_cost` is unchanged from prior `full_rescore`) and populating `down2` + counting NA-block steps. `extract_char_steps` then reads `local_cost` + `down2` + `subtree_actives` + `final_` — all consistent. `compute_weighted_score` dispatches by `scoring_mode`. (3) IW `e/(k+e)` DELTA FORMULA VERIFIED — algebra: `new_cost - old_cost = k/((k+e+1)(k+e)) > 0`. `e<0` guard returns delta=0; `e==0` guard avoids 0/0 NaN when XPIWE `eff_k==0`. (4) PROFILE DELTA CAPPING FIX from 7cff7870 STILL IN PLACE (ts_fitch.cpp:929-948) — three branches (idx_old<0 → 0, idx_old<info_max_steps → table lookup, else → cap at max). Mirrors capping in `compute_profile` (line 910). (5) `precomputed_steps` OFFSET APPLIED in both `compute_profile` (line 901) and `precompute_profile_delta` (line 925) before info_amounts indexing. (6) info_amounts COLUMN-MAJOR INDEXING `[idx + info_max_steps * p]` consistent across both functions. (7) phi/eff_k UNCHANGED since 2026-03-27 review (non-XPIWE: phi=1.0, eff_k=concavity; XPIWE: eff_k=concavity/f, phi=(1+eff_k)/(1+concavity)). `compute_iw` and `precompute_iw_delta` both use `ds.phi[p] * ds.eff_k[p]` consistently. (8) NA DIRTY-SET PASS 2 SUBTLETY VERIFIED — `dirty_up` propagates from root downward; off-dirty siblings of dirty nodes are visited (their parent is dirty), `any_changed` gate avoids spurious cascades. Reverse-postorder ensures parents before children. Tip update (Step 4) reads `anc` final_ from updated values. CORRECT. **No new bugs found.** Next area: 1 (rotation restarts — Fitch scoring correctness).

Previous area: 5
reviewed_by: Claude (cpp-search, 2026-05-26)
date: 2026-05-26
notes: Data pipeline & simplification — ts_data.h/.cpp, ts_simplify.h/.cpp, ts_constraint.h/.cpp, build_reduced_dataset in ts_sector.cpp + ts_prune_reinsert.cpp. **Findings:** (1) LATENT (search-quality, not correctness) — `build_reduced_dataset()` in ts_sector.cpp (lines 421-444) does NOT copy HSJ fields (`hierarchy_blocks`, `tip_labels`, `n_orig_chars`, `hsj_alpha`) or XFORM/Sankoff fields (`sankoff_n_chars`, `sankoff_max_states`, `sankoff_n_states`, `sankoff_cost_matrices`, `sankoff_forced_root`, `sankoff_tip_costs`). Sector internal `score_tree(rd.subtree, rd.data)` dispatches via `rd.data.scoring_mode` (copied verbatim from full ds) and so calls `hsj_score()` / Sankoff with empty hierarchy/Sankoff data — degrading to Fitch-only as a heuristic. Driven search invokes `xss_search/rss_search/css_search` for any scoring mode (no guard at ts_driven.cpp:236,255,278,342,355,371). Post-reinsert full-tree `score_tree(tree, ds)` uses the full ds so final acceptance is correct; only the sector's internal heuristic is wrong. Same class of issue as T-275 (which already guards `prune_reinsert_search` against PROFILE/HSJ/XFORM at ts_prune_reinsert.cpp:497-501). PROFILE *is* fine for sector (info_amounts + info_max_steps + precomputed_steps are all copied). Recommendation: add an analogous guard at the top of `rss_search`/`xss_search`/`css_search` for HSJ and XFORM modes, OR extend `build_reduced_dataset` to copy the HSJ/Sankoff fields. Severity: low (no incorrect scores reported; possible missed improvements / wasted accept-then-revert cycles). NOT auto-fixed (one-line guard but requires testing across all three sector entry points). (2) CONSTRAINT MUST_INSIDE BOUNDARY EDGE — investigated `regraft_violates_constraint` for `below == cn` under MUST_INSIDE: accepted (no rejection because `inside = is_ancestor_or_equal(cn, cn) = true`). Geometrically correct: regrafting clip just above cn creates a new internal node N as a sibling of cn whose tip set = cn's tip set ∪ clip's tips = the original constraint tip set (since MUST_INSIDE means all clip tips were in cn). After the move, `map_constraint_nodes` finds N as the new constraint node. Constraint preserved. CLEAN. (3) DFS TIMESTAMPS in `compute_dfs_timestamps` — iterative DFS with explicit stack, counter monotonic, tips and internals both stamped; ancestor test `entry[u]<=entry[v] && exit[u]>=exit[v]` correct. CLEAN. (4) `classify_clip_constraints` trailing-word remainder masking correct (both `rest` and `out_bits` masked at `cd.n_words - 1`). CLEAN. (5) `build_constraint` canonicalization: n_tips multiple of 64 → no remainder masking needed; remaining bits flipped are all valid. CLEAN. (6) `build_dataset` edge cases — MAX_STATES=32 guard present (B's fix, 2026-03-19); zero-weight patterns erased pre-block-grouping; all-uninformative case → empty blocks, total_words=0, consumed safely by `if (ds.total_words == 0) ...` guards in tbr/drift/ratchet/search; n_states=0 not reachable (n_tokens >= 1 for any real phyDat). CLEAN. (7) T-218 constant-char path: `precomputed_steps[p]` indexed by original pattern; `compute_profile`/`precompute_profile_delta` add it back before info_amounts lookup (with capping at info_max_steps per 7cff7870). CLEAN. (8) IW `min_steps` for removed (weight-0) patterns: `char_steps[p]` stays 0, `extra = -min_steps[p]` ≤ 0 → no IW contribution. CLEAN. (9) `build_reduced_dataset` in ts_prune_reinsert.cpp (lines 298-334): missing same HSJ/Sankoff fields, but T-275 guard at ts_prune_reinsert.cpp:497-501 explicitly skips PROFILE/HSJ/XFORM, so no live issue. (10) Constraint `posthoc_data` built with default concavity (HUGE_VAL = EW); `fitch_score(ctree, posthoc_data)` returns int, comparable to `expected_score`. CLEAN. (11) `flat_blocks` + `all_weight_one` copy in sector build_reduced_dataset (44d929a8) verified present at ts_sector.cpp:429-430. CLEAN. **Tests run (pass):** constraint (916), sector (50), simplify (107). **Where to look next (area 5):** the sector HSJ/XFORM guard once filed; otherwise area is well-covered.

Previous area: 9
reviewed_by: Claude (cpp-search, 2026-05-26)
date: 2026-05-26
notes: Wagner & addition trees + T-302 LengthAdded fix. Scope: src/ts_wagner.h/.cpp, R/AdditionTree.R, R/PolEscapa.R (T-302 fix), tests/testthat/test-PolEscapa.R. **Findings:** (1) BUG FIXED — `R/PolEscapa.R` line 75: `qm <- which(rowSums(cont) == dim(cont)[2])` had the SAME multi-element-vector latent bug as `qmApp` (the T-302 fix target). When the contrast matrix contains multiple fully-ambiguous rows AND a tip has a token where both `app[token]` and `inapp[token]` are TRUE, line 99 `charQm[[leaf]] <- qm` silently corrupts the phyDat by assigning a length>1 integer to a single tip slot. 5 bundled datasets have multi-row `qm` (Asher2005[,10], Conrad2008[,19], Rougier2012[,98], Vinther2008[,1], Wills2012[,77]) but in all of them the trigger condition (`app && inapp` token in actual tip data) is not met, so the bug is latent rather than active. Fix applied: unwrap `qm <- qm[[1L]]` after a length check (matches the T-302 qmApp pattern). 11 PolEscapa tests pass. (2) T-302 FIX VERIFIED CORRECT — `qmApp[[1L]]` unwrap is the right minimal fix; `which()` returns rows in ascending order so "first all-applicable row" is a deterministic, reasonable canonical choice. (3) T-302 NEW TESTS DO NOT EXERCISE THE QMAPP SCORING BUG — Asher2005[,67] has no `-` column (qmApp computation errors there if traced); Vinther2008[,51] and other bundled chars have qmApp of length 0 or 1. The new tests only exercise the contrast-row validity check changes (zeroRows ∩ usedTokens). The qmApp scalar fix itself has no regression test that would fail pre-fix. The original-bug dataset (mbank_X27573, per T-294 record) is not bundled. Recommendation for next reviewer: consider crafting a synthetic phyDat with two all-applicable contrast rows to lock down the fix. (4) BROADER PATTERN SEARCH — grep of R/ shows no other phyDat[[leaf]] assignment with which() output. Pattern is isolated to PolEscapa.R. (5) AdditionTree.R CLEAN — minimal R wrapper around ts_wagner_tree; no scalar issues. (6) ts_wagner.h/.cpp UNCHANGED since prior 2026-05-19 review (which covered NA-incremental staleness, LCA constraint mapping, 3-taxon base case, retry bounds, `final_` init — all CLEAN). **Note:** concurrent rotation pushed last_focus to area 10; this entry is recorded for chronological completeness.

Previous area: 8
reviewed_by: Claude (cpp-search, 2026-05-26)
date: 2026-05-26
notes: Test suite health — focus on T-300 dirty-set coverage, T-302 qmApp regression coverage, lingering flaky/vacuous tests. **Findings:** (1) T-300 DIRTY-SET HAS NO ENDURING REGRESSION TEST — EW + NA dirty-set rescore wired into tbr_search SPR accept (ts_tbr.cpp:1138-1180); DEBUG_RESCORE / DEBUG_NA_RESCORE / DEBUG_NNI_RESCORE cross-checks were removed in 5b210fdd, 44a4ebeb, 2be8228d. Previous incremental attempt produced systematic delta=-3 (b7303ee5 revert). Need a test in spirit of `test-ts-spr-state-restore.R` that drives many SPR accepts (small n, weak signal, many maxHits) and asserts `result$score == ts_score(result_tree, ds)` across EW/IW/NA/NA-IW. NEEDS FILING. (2) T-302 qmApp FIX LACKS POSITIVE-PATH REGRESSION — `test-PolEscapa.R` new assertions only test contrast-row error path; pre-existing `wiwaxia` test was already passing pre-fix. Recommend synthesizing a phyDat with 2+ all-applicable contrast rows. NEEDS FILING. (3) FIXED INLINE — added `set.seed()` before `sample()` at test-ts-tbr-search.R:97/114/135, test-ts-sector.R:159/180, test-ts-drift-search.R:150. 5 seeds. (4) STILL OPEN (low severity, prior review): vacuous OR in test-ts-tbr-search.R:91, test-ts-stopping.R:81. (5) CLEAN — Tier guards consistent; DEBUG scaffolding removal left no orphan tests. **Next area: 9** (Wagner & addition trees + T-302).

Previous area: 6
reviewed_by: Claude (cpp-search, 2026-05-26)
date: 2026-05-26
notes: R↔C++ interface — re-verification after T-302, T-300 DEBUG removal, T-301 isatty fix, fractional-weight R-layer patch. **Findings:** (1) ALL 31 RCPP ARG COUNTS VERIFIED — programmatically counted SEXP args in RcppExports.cpp; 1:1 match with TreeSearch-init.c registration table. (2) `R_PosInf` REGRESSION CHECK CLEAN — concavity defaults in R/RcppExports.R all `-1.0` (mapped to HUGE_VAL in make_dataset). (3) DRIVEN-SEARCH CONCAVITY Inf PATH CLEAN — R passes `as.double(Inf)`; C++ `!std::isfinite(Inf)` triggers EW. (4) START_EDGE WARM-START CLEAN — 1-based→0-based conversion at init_from_edge in both ts_driven.cpp:726 and ts_parallel.cpp:163. (5) RECENT COMMITS DID NOT TOUCH INTERFACE ARITY — T-302 is R-only (PolEscapa.R); fractional weights R-layer; T-300 DEBUG removal C++ only; isatty fix internal. **No new bugs.** Watch for future IntegerVector→NumericVector widening per a204542d. **Next area: 7** (Shiny module wiring).

Previous area: 7
reviewed_by: Claude (cpp-search, 2026-05-26)
date: 2026-05-26
notes: Shiny module wiring — re-review. **No new code in scope.** Last commit touching `inst/Parsimony/` was c5091a88 (2026-05-12), already covered by the 2026-05-18 area-7 review. The recent commits flagged in the brief (1bd1346c PaintCharacters draft, 21484833 Concordance paint-swatch, df7dd541 docs) are pure R-level features — no Shiny module exists for them (verified: `inst/` has zero matches for `PaintCharacters` or `paint_swatch`). **Files re-verified clean:** server.R (cb_ref wiring lines 164-167), server/mod_search.R (search & profile-prep result observers with `tryCatch`/`shiny.silent.error` rethrow/`isolate`), server/mod_data.R (cross-module update imports at 82-84 — dead but harmless), server/mod_consensus.R, server/events.R. All five prior-review findings still hold: (1) cb_ref forward-ref bridge correct (R6-style late binding); (2) Cross-module updateXxxInput targets clean (dead imports harmless); (3) Progress-file 5-field format matches `MaximizeParsimony.R:609-611`; (4) Search result observer pattern correct; (5) Profile prep observer clean. **No bugs found. No fixes applied. Next area: 8 (Test suite health).**

Previous area: 4
reviewed_by: Claude (cpp-search, 2026-05-19)
date: 2026-05-19
notes: Parallelism & RNG — ts_parallel.cpp, ts_rng.h/.cpp, ts_driven.cpp. Prior agent-E review (2026-03-27, area 4) was thorough; this session confirms all E findings remain accurate and reviews the three new additions since that review. **Findings:** (1) R_INTERACTIVE GUARD (lines 431-448) CLEAN — `R_Interactive` extern declared at line 14; guard at line 435 prevents `R_FlushConsole()` in non-interactive mode. `progress_on_line` only set to true in interactive branch — closing `Rprintf("\n")` at line 454 fires correctly. (2) CONSENSUS STABILITY (lines 367-385) CLEAN — `Rprintf` only on main thread; pool `status()` mutex call safe (main thread); `done_now > last_stab_done` guard prevents spurious triggers on idle polls (200ms sleep). (3) PERTURB-STOP RULE (lines 389-423) CLEAN — relaxed atomic reads, all R API calls on main thread only. Dynamic limit formula `(target_hits/hits) * n_tips * psf` uses snapshot of `hits`/`done` that may be slightly stale (relaxed ordering) but directionally safe (conservative, won't stop prematurely). (4) ALL PRIOR E FINDINGS CONFIRMED: seeds pre-generated on main thread with Get/PutRNGstate; worker thread_rng/thread_stop_flag set at entry, cleared at exit; no R API from workers; ctx copied by value with correct thread_id; parallel_resample clean; empty if-block dead code at lines 327-330 still present (harmless). **No new bugs found. Next area: 5** (Data pipeline & simplification).

Previous area: 3 — ts_ratchet.cpp, ts_sector.cpp, ts_fuse.cpp. **Findings:** (1) RATCHET CLEAN — `active_mask`/`upweight_mask`/`flat_blocks[b].active_mask`/`pattern_freq` correctly saved by `save_perturb_state` and restored by `restore_perturb_state`. `perturb_upweight` doesn't need FlatBlock sync (doesn't modify `active_mask`; FlatBlock has no `upweight_mask` field). `use_flat = false` guard at tbr_search:671-674 prevents flat functions during ratchet upweight phase. `n_chars` bounded at 64 (MAX_CHARS_PER_BLOCK=64), so `uint64_t(1) << i` in perturb_zero is safe. Reset to best_tree via `copy_topology` + `build_postorder` + `reset_states` is correct (next tbr_search does full_rescore). (2) SECTOR REVERT PATHS CLEAN — Both RSS and XSS: improvement path (`reinsert_sector` + `build_postorder` + `score_tree`), worsening path (`restore_clade` + `build_postorder` + `score_tree`), constraint-violation path (`restore_clade` + `build_postorder` + `score_tree`). Constraint metadata re-synced on ALL accept paths (improvement + equal-score). CSS delegates to TBR (no manual topology management). (3) FUSE TIED-SCORE CHECK CLEAN — Equal-score acceptance checks that clade topology actually changed (lines 450-458) before accepting. (4) FUSE STALE MARKING DEAD CODE — After accepting one exchange, `improved = true` + stale-split marking (lines 467-480) + `break`. We break the `si` loop immediately, so the stale set is never consulted. The stale marking code is dead. No functional impact (correct behavior: one exchange per round, TBR cleanup follows), but misleading. Low severity. **Next area: 4** (Parallelism & RNG).

Previous area: 2
reviewed_by: Claude (cpp-search, 2026-05-19)
date: 2026-05-19
notes: Search topology invariants — ts_tbr.cpp, ts_drift.cpp, ts_search.cpp. **Findings:** (1) BUG FIXED — `ts_drift.cpp:680`: second `drift_apply_tbr_move` call (EW RFD re-apply path, line 677) missing `drift_restore_topology(tree, snap)` in its failure handler. First apply at line 585 correctly has `drift_restore_topology(tree, snap)` before `build_postorder()`. Second apply lacked it, so a failed re-apply would call `build_postorder()` on a partially-detached tree (step 1 detach ran; step 2 reroot returned false). Path is theoretically unreachable (same args succeed on first apply → succeed on second), but robustness fix applied: added `drift_restore_topology(tree, snap)` at line 680. (2) TBR RESTORE PATHS CLEAN — Phase 2 unclip path (`restore_prealloc_undo` + `spr_unclip` + `postorder.assign(saved_postorder)`) correctly restores states, topology, and postorder after each clip iteration. Apply+reject path (`restore_topology` + `state_snap.restore`) also correct (state_snap includes postorder). Constraint metadata re-synced via `map_constraint_nodes` + `compute_dfs_timestamps` on all reject paths. (3) NNI REJECT PATH CLEAN — `nni_undo` + `clear` + re-run `fitch_incremental_downpass` + `clear` correctly restores prelim/local_cost; stale `final_` is harmless since NNI never reads it for scoring. (4) DRIFT `saved_postorder` NOTED — saved once before clip loop, never updated after accepted moves. After `spr_unclip` + `postorder.assign(saved_postorder)`, postorder may be stale for later iterations if moves were accepted. Harmless: either next clip calls `build_postorder_prealloc`, or accept/validate paths call `build_postorder()` explicitly. Low-priority code quality: could add `saved_postorder = tree.postorder` after each accepted move. (5) HASH COLLISIONS NEGLIGIBLE — Phase 3A virtual_prelim dedup uses 64-bit FNV-like hash; collision probability ~10^-15 per search. Consequence is only a skipped rerooting (missed candidate), not incorrect acceptance. **Next area: 3** (Ratchet & perturbation).

Previous area: 1
reviewed_by: Claude (cpp-search, 2026-05-19)
date: 2026-05-19
notes: Fitch scoring correctness restart — ts_fitch.h/.cpp (incremental path), focus on T-300 root cause and T-245 changes. **Findings:** (1) T-300 ROOT CAUSE FOUND — the systematic delta=-3 bug in `fitch_incremental_downpass(nz) + fitch_incremental_downpass(nx)` is caused by overlapping ancestor paths. When the regraft edge (above, below) places `above` as an ancestor of nz in the new tree, chain1 (nz→root) and chain2 (nx→above→root) both traverse `above`→root. Chain1 correctly updates `above`'s local_cost. Chain2 then subtracts the ALREADY-UPDATED above local_cost (from chain1) rather than the original — giving a delta relative to the chain1 output. Total delta = chain1 correct delta + chain2 double-subtraction error = net undercount. Fix: compute nz's prelim and nx's prelim independently, then do a single walk from LCA(nz,nx) to root using BOTH updated nodes' prelims. Simpler fix: accumulate a "dirty set" = union of paths nz→root and nx→root (sorted by depth), walk once in postorder updating each node exactly once. (2) T-245 (038e00a8) CLEAN — adds `fitch_indirect_cached_flat_x4` and NA variant. These are EW-only (gated `use_flat && !use_iw`). The new functions' logic mirrors the 2-operand `fitch_indirect_cached_flat` extended to 4 independent pointers with bitwise-AND combined cutoff check. Correctness: same block iteration, same intersection/union formula, cutoff initialized to `cutoff_b` for trailing slots (not INT_MAX) — prevents spurious improvement from uninitialized trailing scores. CLEAN. **Next area: 2** (Search topology invariants).

Previous area: 10
reviewed_by: Claude (cpp-search, 2026-05-19)
date: 2026-05-19
notes: Profile & IW scoring — ts_fitch.cpp (IW/profile paths), ts_data.cpp (precompute). Focus: changes since last review (2026-03-27). **Findings:** (1) NO NEW CHANGES to ts_data.cpp or IW/profile paths in ts_fitch.cpp since the prior area-10 review. (2) T-245 (4-wide candidate batch, 038e00a8) adds `fitch_indirect_cached_flat_x4` and `fitch_na_indirect_cached_flat_x4` — gated at ts_tbr.cpp:948 with `if (use_flat && !use_iw)`. IW and profile paths (both set `use_iw = true`) are EXCLUDED from the flat batch. CLEAN. (3) All prior area #10 findings remain valid: `precompute_profile_delta` capping fix (7cff7870) present, `e/(k+e)` formula correct, `precomputed_steps` offset correct, `concavity=1.0` sentinel activates weighted path, `phi`/`eff_k` assignment for XPIWE correct. **No new bugs found. Next area: 1 (rotation restarts)** — Fitch scoring correctness, focusing on changes since 2026-03-27.

Previous area: 9
reviewed_by: Claude (cpp-search, 2026-05-19)
date: 2026-05-19
notes: Wagner & addition trees — ts_wagner.h/.cpp (full review; last changed by T-266 afbf531f). Focus: NA-incremental scoring staleness, LCA-based constraint mapping, retry loop, 3-taxon base case, `final_` initialization. **Findings:** (1) NA-incremental staleness ACCEPTABLE — `fitch_indirect_length_bounded` used for greedy insertion scoring is an EW-only approximation; final score always computed via `score_tree(tree, ds)` (authoritative, handles NA+IW). Documented in code. (2) `final_` initialization SAFE — `fitch_score(tree, ds)` at wagner_tree:450 calls `fitch_uppass()` which sets all `final_` arrays before any `fitch_indirect_length_bounded` calls (which read `final_[a_base]` and `final_[d_base]`). The concern `final_` = zeros at first insertion was investigated and ruled out. (3) LCA-based constraint mapping CORRECT — `wagner_map_constraint_nodes`: first checks tips (handles 1-added-inside-tip case), then checks internal nodes for smaller clade. DFS timestamps rebuilt after every `build_postorder()` call (line 538), always in correct order. Boundary edge check `below != cn` (line 420) allows outside tip as sibling of constraint clade root — correct for monophyly. Root constraint node (cn == n_tip) skipped correctly. (4) 3-taxon base case CORRECT — all unrooted 3-tip trees are isomorphic; specific order irrelevant. (5) Retry loop BOUNDED — 100 attempts in `random_wagner_tree` and `biased_wagner_tree`; can return violating tree on pathological constraints but already documented in area #9 review (2026-03-26). (6) T-266 changes MECHANICAL — only removed anonymous namespace wrappers to expose `init_wagner_state`, `insert_tip_at_edge`, `wagner_incremental_rescore` to ts_prune_reinsert.cpp. No logic changes. **No new bugs found. Next area: 10** (Profile & IW scoring: ts_fitch.cpp IW/profile paths).

Previous area: 8
reviewed_by: Claude (cpp-search, 2026-05-19)
date: 2026-05-19
notes: Test suite health — tests/testthat/test-ts-*.R, helper-ts.R. **Findings:** (1) BUG FIXED — `test-ts-stopping.R` (4 tests) and `test-ts-xpiwe.R` (1 test) each called `data("inapplicable.datasets", package = "TreeSearch")` but then accessed `inapplicable.phyData[["Vinther2008"]]`. `inapplicable.datasets` is a list of lists (tree + matrix format); `inapplicable.phyData` is a separate named list of phyDat objects. These tests only worked when run after another test that had already loaded `inapplicable.phyData`. Fixed by changing both files to `data("inapplicable.phyData")`. (2) NOTED — Several tests use `sample()` without preceding `set.seed()` in the same test block: test-ts-tbr-search.R:97,114,135; test-ts-sector.R:159,180; test-ts-drift-search.R:150. Assertions are conservative (score >= 0, valid topology, score == ts_fitch_score) and should hold for any random data. Low severity: tests are not reproducible but not flaky. (3) NOTED (WEAK ASSERTIONS) — test-ts-tbr-search.R:91: `expect_true(result_equal$n_accepted >= result_strict$n_accepted || result_equal$score <= result_strict$score)` — the second branch (score <=) is almost always true (equal scores satisfy it), making the test nearly vacuous. test-ts-stopping.R:81: `expect_true(result$consensus_stable || result$replicates == 30L)` — OR with max-replicates fallback makes it vacuous. (4) CLEAN — Tier guards correct: tier 3 (bench/stress) files use `skip_extended()`, tier 2 files use `skip_on_cran()`. (5) CLEAN — 3-tip edge case covered: test-ts-wagner.R:280-299 tests 3-tip Wagner tree construction, edge count, score match. (6) CLEAN — All-NA covered: test-ts-na-ambig.R:162 ("all inapplicable" score=0). Single-character covered: test-ts-ratchet-stress.R:117 (tier 3). (7) COVERAGE GAP — No test verifies all intermediate accept-path scores during TBR search are correct. The T-300 DEBUG_RESCORE approach was the right instrumentation strategy; should be reused in a separate test when T-300 is redesigned. **Next area: 9** (Wagner & addition trees: ts_wagner.h/.cpp).

Previous area: 7
reviewed_by: Claude (cpp-search, 2026-05-18)
date: 2026-05-18
notes: Shiny module wiring — inst/Parsimony/server.R, server/mod_*.R, server/events.R. **Findings:** (1) CLEAN — Forward-ref callback bridge via `cb_ref <- new.env(parent = emptyenv())` in server.R; wired at lines 164-167 after all modules initialized. Pattern is correct: R6-style late binding, no forward-reference deadlock possible. (2) CLEAN — Cross-module `updateXxxInput` targets: `UpdateKeepNTipsRange`, `UpdateDroppedTaxaDisplay`, `UpdateOutgroupInput` imported at mod_data.R:82-84 but never called (auto-reactive in mod_consensus.R handles updates). Dead imports — harmless. (3) CLEAN — Progress file format (mod_search.R:860-866): 5 space-separated fields match write format at MaximizeParsimony.R:609-611 exactly. (4) CLEAN — Search result observer (mod_search.R:975-1122): single tryCatch, `shiny.silent.error` re-throw, `isolate()` block. Pattern is correct. (5) CLEAN — Profile prep result observer (mod_search.R:412-449): `isolate()` block, auto-starts search after completion. No orphaned observers. **No bugs found. Next area: 8** (test suite health).

Previous area: 6
reviewed_by: Claude (cpp-search, 2026-05-18)
date: 2026-05-18
notes: R ↔ C++ interface — ts_rcpp.cpp, TreeSearch-init.c, R/RcppExports.R, R/MaximizeParsimony.R. Focus on all 18 exported function registrations and caller paths. **Findings:** (1) ALL ARG COUNTS VERIFIED CLEAN — all 18 entries in TreeSearch-init.c checked against C++ signatures in ts_rcpp.cpp (all match). (2) Concavity sentinel path CLEAN — old interface (C++ default -1.0 → `if (concavity < 0) concavity = HUGE_VAL` in make_dataset:46) and new driven_search interface (R passes `as.double(Inf)` → C++ receives Inf → `!std::isfinite(Inf)` → EW path) both correct. (3) Edge matrix convention CLEAN — `tree_to_edge()` outputs n_edge=2*(n_tip-1) 1-based rows; `init_from_edge()` converts to 0-based internal indexing. `start_edge` warm-start path: stored 1-based in `unpack_runtime`, converted by `init_from_edge` — correct. (4) `.ScaleWeight` fractional weight handling CLEAN — scale=1260 default, overflow guard `if (total > .Machine$integer.max) stop(...)`. (5) R input validation in `MaximizeParsimony` CLEAN — lines 632-698: phyDat class check, min 4 taxa, levels, concavity>0, hierarchy checks all present. (6) FUNCTIONAL LIMITATION (not a bug) — `progressCallback` is NOT invoked in parallel mode (nThreads > 1). Not filed as a task — documented here. (7) ts_resample_search / ts_parallel_resample / ts_successive_approx callers: concavity sentinels handled inline at ts_rcpp.cpp:1629/1735/1852 — correct. **No correctness bugs found. Next area: 7** (Shiny module wiring).

Previous area: 5
reviewed_by: Claude (cpp-search, 2026-05-18)
date: 2026-05-18
notes: Data pipeline & simplification — ts_data.h/.cpp, ts_simplify.h/.cpp, ts_constraint.h/.cpp, ts_sector.cpp (build_reduced_dataset). Focus on changes since B's area-5 review (2026-03-19): 1572fd70 (FlatBlock + flat indirect), T-218 constant-char removal, T-213 impose_constraint. **Findings:** (1) BUG FIXED — `build_reduced_dataset()` in ts_sector.cpp did not copy `flat_blocks` or `all_weight_one` from the full DataSet. `all_weight_one` is read at the top of every `tbr_search` call to gate flat indirect functions (`use_flat = ds.all_weight_one`); when false (the default), flat functions are never called. `flat_blocks.data()` is dereferenced inside those flat functions, so the missing `flat_blocks` was masked by the missing `all_weight_one`. Fixing only one of the two would introduce UB. Fixed both together: added `rd.data.flat_blocks = ds.flat_blocks;` and `rd.data.all_weight_one = ds.all_weight_one;` at ts_sector.cpp after `block_word_offset` copy (lines 429-430). Effect: sector TBR on EW datasets with all-weight-1 characters now correctly takes the `use_flat = true` fast path (4-wide batch + flat bounded functions). Previously always fell through to the slower non-flat path. No correctness regression (both paths give same scores). Note: `ts_prune_reinsert.cpp`'s separate `build_reduced_dataset` already had both fields copied correctly. (2) No other new bugs found. T-213 impose_constraint implementation (ts_constraint.cpp) reviewed — the latent `best_node` stale-reference issue noted in prior area-9 review still applies; not re-filed (already in record). T-218 constant-char removal path in ts_data.cpp/ts_simplify.cpp correctly handles `precomputed_steps` and `extra_steps` — CLEAN. All-ambiguous and single-state edge cases in `build_dataset` gated by `ds.blocks.empty()` guard and block `n_chars == 0` check — CLEAN. **Next area: 6** (R ↔ C++ interface: ts_rcpp.cpp, TreeSearch-init.c, R/RcppExports.R, R/MaximizeParsimony.R).

Previous area: 4
reviewed_by: E
date: 2026-03-27
notes: Parallelism & RNG — ts_rng.h/.cpp (57+53 lines), ts_parallel.cpp (591 lines), ts_parallel.h (141 lines). ts_driven.cpp covered in E-003/S-RED focus ~21 (2026-03-27; see agent-E.md). **Findings:** (1) ts_rng.h/.cpp CLEAN — thread_local pointers init to nullptr and cleared at worker exit. make_rng(): serial wraps unif_rand() in Get/PutRNGstate; parallel seeds new mt19937 from *thread_rng (clean RNG hierarchy). check_interrupt(): serial longjmps via R_CheckUserInterrupt(); parallel relaxed atomic load. thread_safe_unif(): serial calls unif_rand() (caller manages state); parallel uses std::uniform_real_distribution on *thread_rng. rng_state_begin/end correctly no-ops in parallel. (2) ThreadSafePool CLEAN — all methods under lock_guard. fuse_round holds mutex across tree_fuse() (performance concern, not correctness). Passes pool_ (underlying TreePool) to tree_fuse() directly — no nested lock deadlock risk. hits_to_best save/restore logic correct. extract_into correctly called post-join; uses non-collapsed add() for output, overrides hits_to_best with accumulated count. (3) worker_thread CLEAN — ctx copied by value at emplace_back with thread_id already set; each worker gets distinct slot in pre-sized thread_timings/thread_scores arrays (no race). thread_local ts::thread_rng and ts::thread_stop_flag set at entry, cleared at exit. No R API calls from workers. (4) Seeds pre-generated on main thread with Get/PutRNGstate before thread spawn; workers seed local_rng from seeds[rep] read-only. (5) strategies vector declared in parallel_driven_search frame, lives until join() — no dangling pointer. (6) Consensus stability check race: done_now relaxed-read before pool mutex; pool updated before replicates_done incremented in workers → main thread may see done_now slightly ahead of pool, triggering consensus check with fewer trees than expected. Conservative direction — safe. Pool never shrinks so pool_size >= 2 guard from status() call remains valid. (7) MPT enumeration: tbr_search is deterministic (no RNG). thread_rng == nullptr on main thread post-join is correct. (8) parallel_resample: same seed pattern, correct. results[rep] writes all complete before join(). (9) Lines 323-325 in main polling loop: empty if-block (dead code, no action taken). Harmless. (10) Verbosity Rprintf at line 403 acquires pool mutex via status() — could delay interrupt polling if worker holds mutex in fuse_round. Performance concern only. **No bugs found.** Next area: 5 (Data pipeline & simplification: ts_data.h/.cpp, ts_simplify.h/.cpp, ts_constraint.h/.cpp).

Previous area: 3
reviewed_by: F
date: 2026-03-27
notes: Ratchet & perturbation, sectorial search, prune-reinsert (ts_prune_reinsert.cpp 511 lines NEW, ts_ratchet.cpp post-T-273, ts_sector.cpp 7189b02c+33c57518+5ffe2bfa changes). Focus on new/modified code since B's area-3 review (2026-03-19). **Findings:** (1) ts_prune_reinsert.cpp FULLY REVIEWED (new file, never reviewed). T-275 GUARD CORRECT — `prune_reinsert_search()` returns early for PROFILE/HSJ/XFORM modes (build_reduced_dataset copies scoring_mode but not info_amounts/hierarchy_blocks/sankoff_* fields). EW and IW modes proceed correctly. (2) `final_[tip]` INITIALIZATION SAFE — `expand_and_reinsert` calls `init_wagner_state → load_tip_states` which bulk-memcpys ds.tip_states into both `prelim` AND `final_` for ALL tips (including dropped ones). wagner_incremental_rescore uppass skips tips correctly ("tip: final = prelim, already correct"). No uninitialized-memory issue. (3) EW HEURISTIC FOR IW INSERTION — `fitch_indirect_length_bounded` (EW-only) used for greedy tip reinsertion in IW mode. Same approximation as standard `wagner_tree`. Corrected by TBR polish at step 6. Acceptable. (4) ACCEPT THRESHOLD -1e-10: guards against floating-point noise; consistent with TBR convention. (5) TOPOLOGY RENAMING IN `extract_pruned_topology`: swap of root_node↔m in all edges correct (root_node and m are always internal nodes ≥ m; swap is purely rename, no logical structure change). (6) ts_sector.cpp `compute_from_above_for_sector`: CORRECT — total_words sizing consistent (rd.data.total_words = ds.total_words explicitly set; from_above sized to tw=tree.total_words=ds.total_words); path walk from sector_root to root correct; from_above[root]= all-states initialization correct; fitch_join per-step correct. Edge case sector_root==root: empty loop body, from_above = all-states. (7) ts_sector.cpp `compute_node_conflict`: CORRECT — wps/trail_mask correct, trivial split guard (count≤1 || count≥n_tip-1), canonicalization (flip if bit 0 set), FNV-1a lookup consistent with pool hash. `use_weighted` gate (max_w > 1.5) and pick_weights recompute after accepted sector: correct. (8) ts_sector.cpp XSS/CSS adaptive early-exit: CORRECT — `if (result.best_score >= score_before_round) break` fires when score didn't decrease (parsimony minimized → no improvement). (9) ts_ratchet.cpp `check_timeout` forwarding (fafd5d0e): CORRECT — all three tbr_search() calls pass nullptr,nullptr,check_timeout for sector_mask,collect_pool,timeout. (10) T-273 flat_blocks sync: CORRECT — flat_blocks.resize(n_blocks) in build_dataset; loops `for (int b = 0; b < ds.n_blocks; ++b)` safe. **No new bugs found beyond T-275 (already filed and guarded).**

Previous area: 2
reviewed_by: F
date: 2026-03-27
notes: Search topology invariants (ts_tbr.cpp, ts_drift.cpp, ts_search.cpp). Focus on post-C-review commits: T-263 (0edad6e3, snapshot hoisting), T-235 (aafeed21, SPR rescore fix), T-196 (6c531be8, NA+IW screening), 1572fd70 (FlatBlock). **Findings:** (1) T-263 SNAPSHOT HOISTING VERIFIED CORRECT — moving state_snap.save()/save_topology() to once per pass (top of while loop) is safe. Invariant: restore_prealloc_undo()+spr_unclip() returns the clipped tree to EXACTLY the committed state; apply_tbr_move is called on the unclipped committed tree; all rejection paths (validate_topology failure, constraint violation, tabu, worse full_rescore) call restore_topology(snap)+state_snap.restore() which restores from the committed state. StateSnapshot::save/restore does full memcpy of ALL node state arrays (prelim, final_, local_cost, down2, subtree_actives, postorder). No scenario where a candidate modifies the committed state before save. (2) T-235 SPR FIX VERIFIED CORRECT — aafeed21 adds full_rescore(tree, ds) after spr_unclip() on the rejection path in spr_search(). This fixes the stale-state arrays issue identified by C (2026-03-25) and A (2026-03-19). spr_search() now correctly rescores the restored topology before the next iteration. (3) LATENT: flat_blocks.active_mask not updated by ratchet — FlatBlock.active_mask is populated at build_dataset() and NOT updated when ts_ratchet.cpp modifies blocks[b].active_mask (perturb_zero_only line 53, perturb_mixed line 110). If flat indirect functions (fitch_indirect_bounded_flat etc.) are ever wired into dispatch during ratchet TBR, they will use stale active_mask, causing incorrect screening. Currently SAFE — confirmed zero call sites for all flat variants (grep shows declarations+definitions only). Pre-wiring fix required: add flat_blocks sync in restore_perturbation_snapshot(). (4) T-196 NA+IW SCREENING IMPROVEMENT — replacing extract_divided_steps (standard Fitch local_cost for NA) with extract_char_steps (three-pass down2 for NA) gives more accurate per-character step counts for IW delta precomputation on NA datasets. Both operate on partially-updated clipped tree arrays (same approximation class); extract_char_steps is more faithful to the actual NA three-pass score. (5) No bugs filed. 1424+ TBR/driven/SPR/constraint tests pass (from GHA record on T-263 PR).

Previous area: 1
reviewed_by: A
date: 2026-03-27
notes: Fitch scoring correctness (ts_fitch.h/.cpp, ts_fitch_na.h, ts_fitch_na_incr.h, ts_simd.h). Focus on commits since previous focus-1 review (2026-03-19): e7b9b4cf (AVX2), 1572fd70 (FlatBlock + flat indirect), XPIWE, T-229 XFORM fix, 7cff7870 (profile delta). **Findings:** (1) AVX2 dispatch correct: `any_hit_reduce_avx2`, `any_hit_reduce3_avx2`, `fitch_combine_avx2` bit-identical to scalar. `cpu_has_avx2()` thread-safe (C++11 static-local-in-inline + COMDAT ODR = single flag). `fitch_combine_avx2` correctly broadcasts any_intersect/needs_union to 4 SIMD lanes; `storeu256` safe for 8-byte-aligned vectors; no aliasing (left/right/out always distinct tree nodes). (2) Flat indirect functions: infrastructure only (not wired into dispatch, confirmed by grep). Logic matches non-flat exactly — same NA suppression, same `below_actives[b]` scalar indexing. (3) XFORM double-counting: NOT a bug. `non_hierarchy_weights()` zeroes hierarchy weights; `build_dataset()` removes weight=0 patterns (lines 104-109). Fitch blocks contain only non-hierarchy chars; Sankoff handles hierarchy. (4) `fitch_incremental_downpass` stopping condition correct: root always processed before break; self-parent guard prevents infinite loops. (5) `fitch_incremental_uppass` dirty propagation correct: root dirty iff prelim changed; clip_ancestor always dirty. `std::vector<bool>` alloc per clip (~45 bytes at 180 tips) is negligible overhead. **No bugs found.**

Previous area: 10
reviewed_by: A
date: 2026-03-27
notes: Profile & IW scoring (ts_fitch.cpp IW/profile paths, ts_data.cpp precompute). **Findings:** (1) BUG FIXED — `precompute_profile_delta` used `old_cost = 0.0` for both `s <= 0` (correct: invariant character) and `s > info_max_steps` (WRONG: should use capped max-table value). When the divided tree has more steps than the profile table size, the delta was overestimated by the full max-table value, causing overly conservative candidate rejection in profile TBR screening. Conservative bug (no incorrect trees accepted), very low practical impact (only highly homoplastic characters). Fix: added separate branches for s<=0, 0<s<=max, s>max — mirroring the capping logic in `compute_profile()`. Regression test added to test-ts-iw-profile-red10.R. Committed directly to cpp-search (7cff7870). (2) IW `e/(k+e)` delta formula verified correct: `new_cost - old_cost = k / [(k+e+1)(k+e)]`, always positive. `e < 0` guard (below minimum in divided tree) correctly sets delta=0. `e == 0` guard correctly avoids 0/0 NaN. (3) IW `phi`, `eff_k` assignment verified: non-XPIWE uses phi=1.0, eff_k=concavity. XPIWE: eff_k=concavity/f, phi=(1+eff_k)/(1+concavity). (4) PROFILE `concavity=1.0` sentinel correctly activates weighted path via `isfinite()` checks in build_dataset. Profile scoring_mode set before phi/eff_k assignment; phi/eff_k are set to finite values but not used for profile (compute_profile uses info_amounts lookup). (5) `precomputed_steps` offset correctly added in both `compute_profile` and `precompute_profile_delta`. (6) Profile `info_amounts` column-major indexing `[idx + info_max_steps * p]` verified correct. 15 tests pass in test-ts-iw-profile-red10.R.

Previous area: 9
reviewed_by: A
date: 2026-03-26
notes: Wagner & addition trees + constraint impose_constraint (ts_wagner.h/.cpp 595 lines, ts_constraint.h/.cpp 736+144 lines). **Findings:** (1) ts_wagner.h/.cpp: No bugs found. Incremental Wagner scoring (two-pass downpass+uppass) correct. LCA-based constraint mapping (`wagner_map_constraint_nodes`) correct. 3-taxon base case handles all orderings. `biased_wagner_tree()` softmax sampling correct. `random_constrained_tree()` retry logic sound. Recent commits (T-208 biased Wagner, T-213 constraint enforcement, T-214 random constrained tree) all verified. (2) ts_constraint.cpp `topology_spr()`: Root-child case (lines 463-492) correctly handles node absorption when `ns >= n_tip`. Bail-out for `ns` as tip (line 467) is correct. (3) LATENT ISSUE (not filed) — `impose_one_pass()` stale `best_node` reference: when a `move_out_root` is a direct child of `best_node`, `topology_spr` relocates `best_node` (it becomes `nx`). Subsequent `move_in` iterations use `collect_edges_in_subtree(tree, best_node, ...)` which now traverses the wrong subtree (best_node was moved to the regraft location, not the original constraint-matching subtree). **Severity: negligible** — mitigated by: (a) outer retry loop (up to `n_splits+1` passes), (b) safety cap bailout (`n_tip/4+2` moves), (c) all callers have retry/validation logic (Wagner retry loop, random_constrained_tree validation, TBR-level constraint enforcement). Adversarial testing: 80/80 pass (10-tip adversarial × 20 seeds, 30-tip nested × 10 seeds, 8-tip root-straddling × 30 seeds). (4) `regraft_violates_constraint()`: DFS timestamp usage correct — timestamps remain valid for ancestor-descendant testing after clip because non-clipped topology is unchanged. MUST_OUTSIDE boundary check (`inside && below != cn`) correct (allows sibling placement). (5) `classify_clip_constraints()`: Rest-of-tree masking with remainder bit clearing correct for both `rest_has_in` and `rest_has_out`. FORBIDDEN classification (clip straddles + rest straddles) correct. (6) All 902 constraint-related tests pass: impose-constraint(88), constraint-small(23), constraint-multi(806), consensus-constrain(8). No new bugs filed.

Previous area: 4
reviewed_by: D
date: 2026-03-25
notes: Parallelism & RNG (ts_parallel.cpp 535 lines, ts_rng.h 56 lines, ts_rng.cpp 52 lines, ts_driven.cpp 887 lines). **Findings:** (1) BUG FIXED — Consensus stability check in parallel main-thread poll loop called update_consensus_stability() every 200ms poll, not per-replicate. Unchanged counter incremented on idle polls, causing premature termination when replicates are slow (e.g. 2 threads, 30s/rep, consensus_stable_reps=2 would stop after ~600ms of unchanged polls instead of 2 actual replicates). Fix: track replicates_done at last check, only update when new reps complete. (2) FRAGILITY NOTED — R_CheckUserInterrupt() in try/catch (lines 303-308) relies on longjmp being caught by catch(...), which is ABI-dependent (works on Windows/SJLJ, fragile on Linux/DWARF). If longjmp escapes catch, worker threads are orphaned with dangling pointers. Known pattern in R packages; fixing requires R_UnwindProtect which is R >=3.5.0 only. Low priority — works on primary platform. (3) CONFIRMED CORRECT — DataSet and ConstraintData copy constructors produce deep copies (all std::vector members). Thread-safe for parallel worker copies. (4) CONFIRMED CORRECT — Thread-local RNG setup/teardown in worker_thread() and parallel_resample lambda. Seeds generated from R RNG on main thread before spawning. No R API calls from worker threads in normal operation. (5) CONFIRMED CORRECT — ThreadSafePool mutex covers all member functions; extract_into() called only after join. (6) CONFIRMED CORRECT — stop_flag relaxed ordering appropriate (no ordering deps). Benign TOCTOU in hits_to_best convergence check. (7) MINOR — Rf_error() in ts_wagner.cpp:17 is reachable from worker threads if n_tip<3, which would crash. Practically unreachable (DataSet validated before parallel dispatch). (8) COSMETIC — Dead code at lines 290-299 of ts_parallel.cpp (empty if body). (9) MINOR — Multiple threads can trigger fuse_round simultaneously on same done count (correct but wasteful, serialized by mutex).

Previous area: 3
reviewed_by: D
date: 2026-03-25
Previous area: 9
reviewed_by: C
date: 2026-03-20
notes: Wagner & addition trees (ts_wagner.h/.cpp 595 lines, ts_constraint.cpp 350 lines). **Findings:** (1) BUG FIXED — `wagner_edge_violates_constraint` (ts_wagner.cpp) and `regraft_violates_constraint` (ts_constraint.cpp) both used `is_ancestor_or_equal(cn, below, ...)` which returns `true` when `below == cn`. For MUST_OUTSIDE tips/clades, this caused the boundary edge (just above the constraint clade) to be incorrectly rejected. Inserting an outside element there makes it a sibling of the constraint clade and does NOT break monophyly. Fix: added `&& below != cn` to the MUST_OUTSIDE rejection branch in both functions. Search quality improvement (wider valid regraft/insertion set); no correctness impact. (2) Added 2 regression tests to `test-ts-wagner.R` (43 total): constrained random Wagner score verification; constrained sequential Wagner verifying `p1 == p2` (R tips 1,2 are sisters under the constraint). (3) Clarified that `ts_random_wagner_tree` without posthoc data cannot guarantee constraint satisfaction for all addition orders: when inside tips span both sides of the rooted tree's root in the 3-taxon start, `cn == root` triggers the existing guard skipping enforcement. The `has_posthoc` retry loop is the correctness guarantee for `MaximizeParsimony` constrained search. (4) Dead `n_added` param and dead `ew_score` computation noted (not filed). 43 Wagner + 18 constraint + 152 driven-search tests pass.

Previous area: 8
reviewed_by: B
date: 2026-03-20
notes: Test suite health + ParsSim log-space convolution. **Findings:** (1) BUG FIXED — `.LogCumSumExp()` in `R/pp_info_extra_step.r` produced `NaN` instead of `-Inf` when both the running accumulator `Lk[k-1]` and the new value `x[k]` are `-Inf`. IEEE 754: `-Inf - (-Inf) = NaN`, propagating through `abs()`, `exp()`, `log1p()`. Reachable if `MaddisonSlatkin()` returns `NEG_INF` for an interior step count (possible by design per `src/MaddisonSlatkin.cpp`). Fix: added `if (is.finite(x[k]) || is.finite(Lk[k]))` guard; keep `Lk[k] = -Inf` when both are `-Inf` (log(0+0) = -Inf, not NaN). Added 7 targeted assertions. 131 assertions pass. (2) Active-range bounds verified correct for both Carter binary path (`m = 1..split[2]`, all entries finite) and MaddisonSlatkin path (trailing `-Inf` trimmed by `which(is.finite(logP))`). Interior `-Inf` is the only scenario where the NaN could appear. (3) `.ApproxStepInformation` MC branch: exact anchor at `s_min` and MC-based estimates for `s > s_min` are not jointly normalized; cumulative probability could exceed 1 for high-variance characters. Not a correctness bug in current usage (IC values are clamped to 0), but noted.

Previous notes (Shiny module wiring) (server.R 200 lines, mod_data.R 589, mod_search.R 962, mod_consensus.R 1333, mod_clustering.R 295, mod_treespace.R 718, mod_downloads.R 167). **Findings:** (1) Forward-ref callbacks (cb_ref): 4 callbacks wired correctly at server.R:162-166 after all modules initialized. Placeholder closures capture cb_ref environment; actual implementations set synchronously before any reactive fires. ✓ (2) Cross-module updateXxxInput: Only one instance — mod_data.R:203 uses parent_session to update "treespace-relators". Fragile (hardcoded namespace string) but correct. All other updateXxxInput calls use module-local session. ✓ (3) Orphaned observers: None found. All observeEvent/observe blocks reference existing inputs within their module namespace. ✓ (4) isolate() in result observers: search result observer (line 811) has exactly one reactive dependency (searchTask$result()); all cleanup in isolate(). Profile prep result observer same pattern. ✓ (5) New progress polling observer correctly gated: checks progressFile(), r$searchNotification, r$searchInProgress before polling. Uses separate reactiveVals from profile prep observer. invalidateLater(500) only scheduled when all gates pass. ✓ (6) ShowConfigs operates on top-level DOM IDs (not namespaced) — correct since UI elements like "whichTree", "consConfig" are defined in ui.R outside modules. ✓ (7) UpdateActiveTrees reentrancy guard (r$updatingTrees) uses on.exit for cleanup — correct. ✓ (8) Change detection pattern (r$oldkeepNTips, r$oldOutgroup, etc.) prevents reactive cascades from programmatic input updates — correct but complex. ✓ No new bugs or tasks filed.

Previous: area: 5
reviewed_by: B
date: 2026-03-19
notes: Data pipeline & simplification (ts_data.h/.cpp 289 lines, ts_simplify.h/.cpp 370 lines, ts_constraint.h/.cpp 350 lines). **Findings:** (1) FIXED — `build_reduced_dataset()` in ts_sector.cpp did not copy `ds.inapp_state` to the reduced dataset. Default value (-1) meant "no inapplicable state," which is currently harmless because sectors use Fitch scoring (EW/IW/PROFILE) which reads `blk.has_inapplicable` per block. Would be a bug if sectors were extended to support HSJ scoring. Added `rd.data.inapp_state = ds.inapp_state;` after the existing field copies. (2) FIXED — No guard for `n_states > MAX_STATES (32)` in `build_dataset()`. Token bitmasks use `uint32_t`, so `(1u << s)` for s >= 32 is undefined behavior. Added `Rf_error()` check at top of `build_dataset()`. Unlikely in practice (morphological data: 2–10 states; DNA: 4; protein: 20) but defensive. (3) NOT FILED — `build_reduced_dataset()` does not copy HSJ/Sankoff fields (hierarchy_blocks, tip_labels, sankoff_*). Same analysis as #1: sectors don't use those scoring modes. Not worth fixing until sectors support HSJ/XFORM. (4) Simplification correctness verified: uninformative-character detection (classical criterion + 4-caterpillar verification for ambiguous tokens) is correct and conservative. All-inapplicable characters are not simplified (skipped in Phase 1) but score 0, so no correctness impact. (5) Constraint column-major indexing verified: `split_matrix[s + n_splits * t]` correct. Canonicalization (tip 0 outside) handles n_tips as multiple of 64 correctly (remainder=0 skips clearing, all bits valid). DFS timestamp allocation matches tree node count. `regraft_violates_constraint` logic verified for MUST_INSIDE/MUST_OUTSIDE. (6) EW offset interaction with IW/Profile verified: uninformative patterns (removed from blocks) contribute 0 to IW score (extra_steps clamped to 0) and correct constant to Profile score (precomputed_steps added back before lookup). (7) Added 10 new test assertions: RSS + XSS + sector_diag with inapplicable characters. All 1679 ts-* pass.

Previous: area: 4
reviewed_by: E
date: 2026-03-19
notes: Parallelism & RNG (ts_parallel.cpp 450 lines, ts_rng.h/.cpp 110 lines, ts_driven.cpp 484 lines). **Findings:** (1) Thread-local RNG: ✓ `ts::thread_rng` and `ts::thread_stop_flag` set before replicate loop at line 96–97 of ts_parallel.cpp. Cleaned up at line 153–154. (2) No R API from workers: ✓ All Rprintf calls in ts_driven.cpp gated by `verbosity >= 2`; parallel calls pass `verbosity = 0` (line 126). All interrupt checks via `ts::check_interrupt()` which dispatches to `thread_stop_flag`. Wagner uses `thread_safe_unif()` and `rng_state_begin/end()`. HSJ and Sankoff modules have zero R API calls. (3) Pool mutex: ✓ All ThreadSafePool public methods hold lock_guard. `fuse_round()` holds lock during tree_fuse + score_tree + pool.add. (4) Atomic stop flag: ✓ Uses `memory_order_relaxed` throughout, fine for simple boolean flag. (5) Seeds: ✓ Pre-generated from R RNG on main thread (lines 190–194, 385–389), `GetRNGstate/PutRNGstate` brackets. (6) DataSet copy correctness: ✓ All HSJ/Sankoff fields (hierarchy_blocks, tip_labels, sankoff_cost_matrices, etc.) are `std::vector`s — default copy constructor deep-copies. (7) HSJ/Sankoff thread safety: ✓ Both `hsj_score()` and `sankoff_score()` are stateless (only use parameters and local variables). No global mutable state. (8) **PERF NOTE** (not a bug): XFORM scoring rebuilds `SankoffData` in every `score_tree()` call (vector allocation in hot path). Could pre-build and cache in DataSet. Not filed as a bug since it's optimization-only and XFORM is new/experimental. (9) init.c: 45 entries (43 Rcpp + 2 manual), all arg counts match. (10) Score verification: serial=80→TreeLength=80 ✓, parallel=79→TreeLength=79 ✓ (Vinther2008). No new bugs found.

Previous: area: 2 (complementary)
reviewed_by: A (complementary to B's focus 2 review)
date: 2026-03-19
notes: Search topology invariants — deeper analysis of state restoration. **Findings:** (1) FOUND — SPR stale scoring arrays after rejected regraft. In `spr_search` (ts_search.cpp), after `spr_regraft + full_rescore + rejection + spr_unregraft + spr_unclip`, the `restore_saved_states()` only restores nodes on the clip-to-root path (saved during incremental pass). Nodes on the regraft-to-root path that aren't on the clip-to-root path retain prelim/final_/local_cost values from the regrafted topology's `full_rescore`. On subsequent clip iterations, `fitch_incremental_downpass` may read stale prelims for nodes below the new clip ancestor, producing incorrect `divided_length` and indirect evaluations. **Impact: conservative only** — stale arrays affect candidate screening but never acceptance decisions (all moves gated by `full_rescore` verification). Final score always correct (line 374 does `full_rescore`). Confirmed by targeted test: `test-ts-spr-state-restore.R` (33 assertions, EW/IW/NA datasets × multiple starting trees). **Not filed as a bug** because SPR is a secondary search method (TBR is primary in driven pipeline) and the issue is self-correcting over multiple passes. (2) TBR (ts_tbr.cpp): All rejection paths fully correct. Phase 2 restore via `restore_prealloc_undo() + spr_unclip() + saved_postorder` restores arrays exactly. Candidate verification via `save_topology + state_snap.save → apply_tbr_move → full_rescore → reject → restore_topology + state_snap.restore` restores topology + all arrays including postorder. Tabu rejection path identical. `states_valid` flag is dead code (always true) — both branches call `full_rescore` so no functional impact. (3) NNI (ts_search.cpp): Standard path: `nni_undo + incremental_downpass` correctly restores prelim/local_cost (second downpass recomputes from restored topology's children). final_ stale after rejection but unused until next acceptance (uppass only on accept). NA path: `score_tree` always does full recomputation, so stale arrays are irrelevant. ✓ (4) Drift (ts_drift.cpp): B's `saved_postorder` fix (line 676-678) verified correct. RFD computation's double-rescore-and-reapply pattern is correct if expensive. `drift_search` outer loop always starts phases with `full_rescore`. Subtree sizes computed once in drift_phase (not updated after moves) — minor search-quality concern, not correctness. (5) Hash collision risk: 64-bit hash, birthday bound ~k²/2^64. For k=10000 rerootings, P(collision) ≈ 5×10⁻¹². Negligible. Test added: test-ts-spr-state-restore.R (Tier 2).

Previous: area: 3
reviewed_by: B
date: 2026-03-19
notes: Ratchet & perturbation (ts_ratchet.cpp 237 lines, ts_sector.cpp 792 lines, ts_fuse.cpp 522 lines). **Findings:** (1) BUG FIXED — `perturb_upweight()` and `perturb_mixed()` in `ts_ratchet.cpp` used `ds.pattern_freq[pat] *= 2` per selected character. When multiple characters share the same pattern index, this gives exponential blowup (`original * 2^N` instead of `original + N`). Only affects IW/profile perturbed landscape (EW uses upweight_mask independently, IW uses pattern_freq exclusively). Could cause integer overflow with highly compressed datasets and high perturbation probability (adaptive tuning at 0.5, 50+ chars sharing a pattern). Fix: changed `*= 2` to `+= 1` (additive, consistent with EW upweight_mask semantics). (2) Ratchet save/restore: PerturbSnapshot correctly saves/restores active_masks, upweight_masks, and pattern_freq vector. Unperturbed search always uses original weights. Best-tree topology reset uses copy_topology + build_postorder + reset_states. ✓ (3) upweight_mask has NO effect on IW/profile scoring: fitch_downpass uses upweight_mask for EW step count, but extract_char_steps, compute_iw, compute_profile, precompute_iw_delta, and all indirect_iw_length variants only use pattern_freq. Setting upweight_mask in IW perturbation is harmless but redundant. (4) Sectorial reinsertion revert: RSS/XSS save CladeSnapshot before reinsertion, full-tree rescore after, revert if score worsens. Post-hoc constraint check with revert. All paths maintain valid final_ states for HTU construction. ✓ (5) XSS sectors from same partition are independent (non-overlapping tip sets). Accepting one sector can't corrupt another. (6) CSS uses sector_mask in TBR (no HTU approximation); no revert needed (TBR handles internally). ✓ (7) Fuse tied-score handling: accepts equal-score exchanges only with accept_equal AND actual topology change (prevents infinite loops). Undo on rejection correctly restores clade left/right/parent. ✓ (8) Fuse ancestor stale marking (lines 467-480) is dead code — loop breaks immediately after acceptance. Harmless defensive code. (9) Minor: RSS doesn't update eligible list after equal-score acceptance (stale subtree sizes); efficiency concern only. 1404/1404 ts-* pass.

Previous: area: 2
reviewed_by: B
date: 2026-03-19
previous_notes: Search topology invariants (ts_tbr.cpp, ts_drift.cpp, ts_search.cpp). **Findings:** (1) BUG FIXED — `drift_phase()` in `ts_drift.cpp` sets `saved_postorder` once (line 404) and never updates it after accepted moves. When the last clip candidate is rejected, the stale postorder is restored (line 566). The subsequent `tbr_search` (called from `drift_search`) starts with `full_rescore()` which uses `fitch_downpass()` iterating over the stale postorder — processing nodes before their children, producing wrong scores. Impact: first few TBR iterations after drift perturbation have incorrect accept/reject decisions. Self-healing once a move is accepted (build_postorder_prealloc rebuilds). Fix: added `tree.build_postorder()` before return when n_accepted > 0. (2) TBR search (ts_tbr.cpp): Topology save/restore is correct. TopoSnapshot + StateSnapshot used for rejection. StateSnapshot includes postorder (line 263). PreallocUndo used for clip/unclip cycle. saved_postorder kept current by StateSnapshot.save() at line 830 (before each candidate apply). `states_valid` flag is dead code (always true) — minor smell. (3) SPR search (ts_search.cpp): Correct — `build_postorder()` called after every clip/unclip at line 360, regardless of accept/reject. (4) Hash dedup in TBR: 64-bit FNV-1a for virtual_prelim; collision risk negligible. (5) Verified: `apply_tbr_move` / `drift_apply_tbr_move` reroot path is correct for path reversal. All 1397 ts-* pass.

Previous: area: 1
reviewed_by: C+A
date: 2026-03-19
previous_notes: Fitch scoring correctness. **C's review:** Thorough review of ts_fitch.h/.cpp (711 lines), ts_fitch_na.h (290 lines), ts_fitch_na_incr.h (678 lines). Findings: (1) BUG FIXED — `fitch_indirect_length_bounded` and `fitch_indirect_length_cached` did not account for `upweight_mask`, underscoring candidates during ratchet perturbation TBR. The NA-aware variants (`fitch_na_indirect_length_bounded/cached`) were already correct. Also fixed `nx_cost` computation in ts_tbr.cpp, ts_search.cpp, and ts_drift.cpp, plus drift RFD computation — same missing upweight_mask pattern. Impact: TBR screening during ratchet perturbation was slightly inaccurate. Final result correctness unaffected (full_rescore() is authoritative). All 1397 ts-* pass. (2) fitch_downpass_node standalone function correctly omits upweight_mask (callers handle it). (3) Incremental downpass stop condition correct (root check + self-parent guard). (4) Incremental uppass dirty propagation correct — clip_ancestor marked dirty, tips processed separately in NA path. (5) NA three-pass algorithm (Brazeau et al.) correctly implemented: Pass 1 NA-aware prelim, Pass 2 uppass with applicability propagation (tips processed separately), Pass 3 corrected scoring with subtree_actives. (6) extract_char_steps correctly uses raw local_cost bits (not upweight-adjusted) for per-pattern counts. (7) fitch_indirect_length uses union-of-finals (correct for non-additive; Goloboff 1996) rather than intersection-then-union. **A's complementary review:** (A1) PROVED mathematically: standard Fitch incremental uppass dirty-flag logic is correct even when downpass stops before root. When downpass stops at node N (prelim unchanged), all nodes below N on the path have correct finals without explicit recomputation. Proof: if fitch(M_old, S) = fitch(M_new, S) and both are intersection-type, then N_final ⊆ M_old ∩ S = M_new ∩ S, so uppass(N_final, M_old) = uppass(N_final, M_new) = N_final. For union cases, fitch(M_old∪S) = fitch(M_new∪S) requires M_old = M_new when M∩S = ∅. (A2) FOUND: NA-aware uppass has a theoretical children_app staleness. The NA uppass formula uses `children_app = OR(children's prelims)` which can change even when the parent's prelim is stable — because the NA downpass prelim formula aggregates differently than raw OR. Specifically, case_children depends on children_app, which can differ if a child's prelim changed. This means the stopping node's final_ for NA blocks could be stale. Affects `fitch_na_pass3_score` divided_length. Conservative: full_rescore always catches it. Same class of design choice as extract_divided_steps heuristic. (A3) Confirmed C's upweight_mask fixes are correct. All 1397 ts-* pass.

Previous: area: 2
reviewed_by: C
date: 2026-03-25
notes: Search topology invariants — thorough review of ts_tbr.cpp (1020 lines), ts_drift.cpp (812 lines), ts_search.cpp (426 lines), ts_tree.h/.cpp (602 lines). **Findings:** (1) BUG FOUND (T-235) — `spr_search()` stale state arrays after rejected regraft. When a candidate passes indirect screening but `full_rescore` shows it's not actually better, the unclip path only partially restores states: `full_rescore()` at line 374 overwrites ALL node states for the regrafted topology, but `spr_unclip()` at line 396 only restores the clip-to-root incremental saves. Subsequent clips use stale divided_length baselines. Impact: degraded screening for NA/IW datasets through SPR path. Final score always correct (full_rescore at exit). Fix: add `full_rescore(tree, ds)` after `spr_unclip()` on the rejection path. Low practical impact — all presets disable `sprFirst`, and SPR is only called in driven pipeline for constrained+sprFirst. (2) TBR uses correct pattern: unclips BEFORE verification, then saves/restores full StateSnapshot around apply_tbr_move. No stale-state issue. (3) drift_phase: `saved_postorder` set once and never updated after accepted moves — KNOWN, documented in comments (lines 725-731), harmless (postorder always rebuilt before use). (4) drift_phase: subtree_sizes computed once before loop, not updated after accepted moves — heuristic filter only, doesn't affect correctness. (5) TBR `states_valid` variable: initialized true, only ever set to true. The `!states_valid` branch at line 1006 is dead code. (6) TBR FNV-1a hash dedup for virtual_prelim: collision probability negligible with ~50 words. (7) NNI search: non-NA path correctly restores prelim/local_cost via re-downpass after rejected NNI; final_ staleness harmless (only downpass used for evaluation). NA path: `score_tree()` does full downpass+uppass, so stale states don't propagate. (8) Verified constraint enforcement paths in TBR and drift: post-hoc `map_constraint_nodes` check correctly catches TBR rerooting violations; restoration path correctly calls `update_constraint` after revert.
EOF 2>&1
