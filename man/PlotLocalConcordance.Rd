% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/LocalConcordance.R
\name{PlotLocalConcordance}
\alias{PlotLocalConcordance}
\title{Plot local concordance}
\usage{
PlotLocalConcordance(
  lc,
  dataset = NULL,
  cols = NULL,
  zlim = NULL,
  mar = c(0.5, 4, 0.5, 1),
  ...
)
}
\arguments{
\item{lc}{A matrix returned by \code{\link[=LocalConcordance]{LocalConcordance()}}.}

\item{dataset}{Optional \code{phyDat} object; if provided, the alignment is drawn
as a bottom panel.}

\item{cols}{Colour ramp function (or vector) for concordance scores.
Defaults to a red--white--blue diverging palette.}

\item{zlim}{Numeric vector of length 2 giving the score range mapped to the
colour scale.  Defaults to the range of finite values in \code{lc}.}

\item{mar}{Numeric vector of length 4; base-graphics margins for each
concordance panel.}

\item{\dots}{Additional arguments passed to \code{\link[=image]{image()}}.}
}
\value{
Invisibly returns \code{lc}.
}
\description{
Displays a multi-scale local concordance summary alongside (optionally) the
alignment itself, using base graphics.
}
\examples{
data("congreveLamsdellMatrices", package = "TreeSearch")
dataset <- congreveLamsdellMatrices[[1]]
lc <- LocalConcordance(dataset, sigma = c(5, 10, 25))
PlotLocalConcordance(lc, dataset)

}
\seealso{
\code{\link[=LocalConcordance]{LocalConcordance()}} to compute scores.
}
\author{
\href{https://smithlabdurham.github.io/}{Martin R. Smith}
(\href{mailto:martin.smith@durham.ac.uk}{martin.smith@durham.ac.uk})
}
