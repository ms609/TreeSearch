% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/CharacterHierarchy.R
\name{hsj_absent_state}
\alias{hsj_absent_state}
\title{Identify the primary "absent" state for HSJ scoring}
\usage{
hsj_absent_state(dataset)
}
\arguments{
\item{dataset}{A \code{phyDat} object.}
}
\value{
Integer scalar: the 0-based index of the absent token in
\code{attr(dataset, "levels")}.
}
\description{
Returns the 0-based token index of the primary character's \emph{absent} state,
for the \link{recode_hierarchy} scorer's \code{absent_state} argument.
}
\details{
Under reductive coding (Hopkins & St. John 2021) the controlling primary
character codes presence/absence of a structure, conventionally \code{"0"} =
absent, \code{"1"} = present.  The index of \code{"0"} depends on the dataset's
\code{levels} ordering (e.g. it is 1 for \code{c("-", "0", "1")} but 0 for
\code{c("0", "1")}), so it must be computed rather than hard-coded.  The
inapplicable token \code{"-"} is \emph{also} treated as absent by the C++
scorer; if no \code{"0"} level exists, its index is returned as the absent
state.
}
\keyword{internal}
