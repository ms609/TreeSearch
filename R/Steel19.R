#' @rdname Carter1
#' @details
#' `Steel19()` implements the $O(n^2 4^c)$ recursive formula for computing the
#' probability that a tree with states `c` has length `L` given as equation 19
#' in \insertCite{Steel1996;textual}{TreeSearch}.
#' @export
Steel19 <- function(steps, states) {
  
}
