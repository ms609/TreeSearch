# Red-team findings — TreeSearch (OPEN)

Verified, non-trivial red-team findings that are **not yet resolved**. Trivial issues are
fixed inline during the round (and noted in `log.md`), never filed here. A finding is
filed here only **after** verification (see the `red-team-verifier` step in `/red-team`).

**Relationship to `to-do.md`:** the root `to-do.md` is the *dispatcher's* operational
queue (all task types). This file is the *red-team* view of the same open findings, so the
next rotation round can see at a glance what's already filed and avoid re-reporting it. When
a finding lands (PR merged), **remove the row** — resolved history lives in git, in
`to-do.md`/`completed-tasks.md`, and in the round entry in `log.md`. Do not keep a resolved
"trophy" table here.

Severity key: **P1** = wrong user-visible result / crash / desk-reject · **P2** = wrong on
edge input / frozen-shape inconsistency / search-quality · **P3** = robustness / polish.

| ID | Sev | Area | Title | File:line | Detail |
|----|-----|------|-------|-----------|--------|
| T-309 | P2 | 7 (Shiny) | **FIXED (78b74147, 2026-06-19) — awaiting `tidy` archive.** EasyTrees: stale profile dataset scores wrong trees | `inst/Parsimony/server/mod_search.R:440` | On `profilePrepTask` completion the code runs `profileDataHash(r$dataHash)`, stamping the *current* dataset hash at completion time, not the hash of the dataset actually prepared. Load H2 while prep runs on H1 → `profileDataHash=hash(H2)` but `profileDataset=preparedFrom(H1)`; the `StartSearch()` guard (`:640`) then skips re-prep and `scores()` (`:475`, no hash check) scores H2 trees against the H1 profile dataset → wrong profile scores shown. `observeEvent(r$dataset)` (`:1128`) never clears `profileDataset/profileDataHash`. Fix: stamp the hash of the prepared dataset (snapshot at invoke), clear both on data change. Verified REAL (opus). Needs a mid-prep data swap to trigger. |
| T-310 | P2 | 7 (Shiny) | **FIXED (78b74147, 2026-06-19) — awaiting `tidy` archive.** EasyTrees double-launch: no `searchInProgress` guard | `inst/Parsimony/server/mod_search.R:632` | `StartSearch()` lacks a re-entrancy guard; `shinyjs::disable("go")` is an async round-trip, so a fast double-click fires `observeEvent(input$go, StartSearch())` twice. Verified vs shiny 1.13.0 `ExtendedTask`: `invoke()` while running *queues* the 2nd call, which overwrites the single `cancelFile()`/`progressFile()` reactiveVals and `r$searchNotification` (leaks 1st notification — no `removeNotification` at `:719`) and re-enables Go mid-flight; on settle, task 1's trees may be silently dropped. Fix: `if (isTRUE(r$searchInProgress)) return(invisible())` at the top. One-line. Verified REAL (opus). |
| T-311 | P3 | 7 (Shiny) | **FIXED (78b74147, 2026-06-19) — awaiting `tidy` archive.** EasyTrees: session disconnect never cancels the running worker | `inst/Parsimony/server.R:187` | `onStop` cleans file caches + cmd log but never writes the `cancelFile()` signal the `future::future()` worker polls (`mod_search.R:710`). A mid-search disconnect leaves the worker burning a core until it finishes its replicates or hits the timeout (~60 min for "thorough"). Fix: write the active cancel signal in `onStop` (or expose a module `cancel()`). Verified REAL (haiku). |
| T-312 | P3 | 7 (Shiny) | **FIXED (78b74147, 2026-06-19) — awaiting `tidy` archive.** EasyTrees: search temp files (`ts_*`) leak on session end | `inst/Parsimony/server.R:192-194` | `onStop`'s `unlink(pattern="^(data\|tree\|excel)File-")` doesn't match the temp files `mod_search.R` creates: `ts_cancel_*`, `ts_progress_*`, `ts_profile_prog_*`, `ts_profile_cancel_*`. The worker `on.exit` clears some on the normal path; on error/interrupt/disconnect they accumulate in `tempdir()` (the documented "Issue 6" tempdir growth in `../expertise/shiny-app.md`). Fix: add a `ts_(cancel\|progress\|profile_prog\|profile_cancel)_` unlink to `onStop`. Verified REAL (haiku). |
| T-313 | P3 | 7 (Shiny) | **FIXED (78b74147, 2026-06-19) — awaiting `tidy` archive.** EasyTrees: topology dedup includes branch lengths → inflated pool | `inst/Parsimony/server/mod_search.R:1063-1066` | The "topology string" dedup uses `write.tree(ape::ladderize(t))`, but `write.tree()` serialises branch lengths when present. After `combined <- c(r$allTrees, newTrees)` mixes user-loaded trees (which may carry BLs) with parsimony trees (no BLs), topologically identical trees with different BLs aren't deduplicated → inflated pool and displayed tree count. Fix: strip branch lengths before serialising (drop `$edge.length`, or use a topology-only key). Verified REAL (haiku). |
| T-322 | P3 | 8 (Tests) | Wagner NA+IW regression test is tautological (omits `min_steps`) | `tests/testthat/test-ts-wagner.R:223-242` | The test "Wagner on NA + IW matches fitch_score" calls `ts_random_wagner_tree(...)` and `ts_fitch_score(...)` both with `concavity = k` but **omits `min_steps`** (defaults to `integer(0)`), so the implied-weight homoplasy `h = steps − min_steps` is computed as `h = steps − 0` on *both* sides. The cross-check (Wagner incremental score == independent Fitch rescore of the same tree) therefore passes, but validates a non-production formula: the real NA+IW path (`R/MaximizeParsimony.R:834`) always passes `min_steps = as.integer(MinimumLength(ds, compress = TRUE))`, and Vinther2008 has inapplicable characters so `MinimumLength` is non-zero — a regression in NA+IW `min_steps` handling would slip through. Fix: pass `min_steps = as.integer(MinimumLength(pd, compress = TRUE))` to *both* calls (the fn accepts it, RcppExports.R:147) and re-run; the cross-check stays valid (same `min_steps` both sides) while now exercising production scoring. Verified REAL (sonnet). **FIXED 2026-06-16 (d93b0a34)** — both calls now pass `min_steps`; test re-run green. (Kept here until merged.) |
| T-323 | P2 | 9 (Wagner) | Wagner kernel has no length/range guard on `addition_order` (OOB / segfault) | `src/ts_rcpp.cpp:855-862`, `src/ts_wagner.cpp:433-470` | `ts_wagner_tree` does `order[i] = addition_order[i] - 1` with **no** length/range validation; `wagner_tree` trusts `order` is a length-`n_tip` permutation and reads `order[0..2]`/`order[i]` for `i ∈ [3, n_tip)`. A short vector reads past its end — **hard segfault reproduced**: `TreeSearch:::ts_wagner_tree(..., addition_order = c(1L))` → exit 139 in `build_three_taxon_tree`. An out-of-range index (0→-1, or >`n_tip`) indexes `tree.parent[]`/`tip_states[]` OOB (heap **write** UB); a same-length non-permutation yields a malformed tree (tip duplicated, another dropped). Same OOB class as WGN-01 (PR #252), which guarded the *public* `AdditionTree(sequence=)` R path but left the C++ kernel unguarded. `ts_wagner_tree` is **not exported** (reachable only via `TreeSearch:::`), so not via the public API → P2. Fix: Rcpp-boundary guard — `addition_order` empty or a length-≥`n_tips` permutation of `1..n_tips`, `Rcpp::stop` otherwise (mirror existing `weight`/`levels`/`min_steps` checks). Verified REAL (opus): segfault exit 139 + malformed-tree reproductions. |
| T-324 | P3 (→P2 if reachable) | 9,13 (Wagner/Constraint) | Wagner retry-exhaustion returns a constraint-violating start that the `MaximizeParsimony()` search driver captures **ungated** (no verify-before-capture, asymmetric to fuse) | `src/ts_wagner.cpp:745-754, 784-797` + `src/ts_driven.cpp:929` | If all 100 retry attempts still `violates_constraint_posthoc`, `biased_wagner_tree` (`:745-754`) / `random_wagner_tree` (`:784-797`) return the last (violating) tree with **no warning** — unlike the per-taxon edge-exhaustion path, which warns (`constraint_fallback`, `:571-576`). **Area-13 (2026-07-03) deepened this from a warning-parity gap to a conditional correctness gap on the `MaximizeParsimony()` search path (T-324's original framing was `AdditionTree()`-scoped):** (1) the per-replicate pool capture at `ts_driven.cpp:929` (`pool.add_collapsed(rep_result.tree, …)`) has **NO** constraint gate — asymmetric to the fuse capture 100 lines below (`:1042-1058`), which maps constraint nodes, imposes, and discards on residual violation (`fused_ok`). (2) A violating start is **not repaired** by constrained TBR (`regraft_violates_constraint` returns true for all moves once `constraint_node[s]<0`, `ts_constraint.cpp:354-360` → tree freezes); `nni_perturb_search`'s `impose_constraint` (`ts_driven.cpp:412-419`) can repair it **only if** `nni_perturb_per>0` and the heuristic succeeds — otherwise the violating start survives to `:929`. (3) **No downstream filter** (confirmed): `ts_rcpp.cpp` (post-`:1390`) and `MaximizeParsimony.R` post-search (collapse-protection only, `:1028-1034`) never re-check constraint satisfaction → a violating tree is returned to the user unflagged. **Severity: P3 on the proven inconsistency (missing gate + missing warning); escalates to P2 (silent wrong answer for a *valid* user constraint) IFF reachability is confirmed** — a satisfiable user constraint whose violation survives all 100 independent reshuffles. The retry loop's existence proves pass-construction/fail-posthoc trees exist; the open bit is 100-reshuffle persistence. Recommend a Hamilton hard-but-satisfiable-constraint probe to settle reachability (not local — heavy compute). **Fix (do NOT mis-patch):** the `:929` gate must use `violates_constraint_posthoc(tree, *cd)` for a user constraint (`has_posthoc=true`), **NOT** the fuse-style `constraint_node[s]<0` check — a posthoc-only violation (all `cn>=0` but fails full-Fitch) is exactly the case the posthoc DataSet exists for and would slip a `constraint_node`-based gate. Also emit the missing `Rf_warning` on retry-exhaustion in both Wagner paths (original T-324 ask). Only user constraints reach `:929` with `has_posthoc=true`; auto-consensus `auto_cd` is `has_posthoc=false` and engages only when **no** user constraint (`ts_driven.cpp:724`), so it is a search heuristic, not a user-facing violation. Verified REAL by orchestrator source-trace at HEAD 4b833e7f (opus); reachability UNCONFIRMED. Sibling of T-329 (same constraint machinery — coordinate fixes). |
| T-328 | P2 | 9 (Wagner) | Wagner kernel has no range guard on `tip_data` values (OOB / segfault) — sibling of T-323 | `src/ts_simplify.cpp:194-199`, `src/ts_data.cpp:45` | `simplify_patterns` builds per-tip bitmasks: `int token = tip_data_r[tip + n_tips*p] - 1` (`:194`) with **no** range check on the `tip_data` value, then indexes `token_states[token]` (`:195`, `:198-199`). `token_states` is sized `n_tokens` (`ts_data.cpp:45`), so a `tip_data` value of 0 → `token = -1` (OOB read one-before-start — usually a silent misread) and any value > `n_tokens` → OOB read past the end → likely SIGSEGV. `make_dataset` guards vector *lengths* / `n_states` (`ts_data.cpp:28`) but **not** the `tip_data` *values* — a distinct gap from T-323 (which guards `addition_order`, a different vector on the same Rcpp boundary). Reached from every Wagner entry point (`make_dataset`→`build_dataset`→`simplify_patterns`). `ts_wagner_tree` is **not exported**; public `AdditionTree()`/`MaximizeParsimony()` build `tip_data` from a validated phyDat (values are valid 1-based contrast-row indices by construction, `AdditionTree.R:95-98`) → reachable only via `TreeSearch:::ts_wagner_tree()` with a hand-crafted matrix → **P2 with a P1 tail** (a public transform dropping a contrast row without reindexing the data — `PrepareDataProfile`/`PrepareDataIW`/`.Recompress` — would desync indices from rows and inject the OOB on a public path; flagged as a follow-up, not hunted this round). Same OOB class as T-323 → fix together: an Rcpp-boundary guard that every `tip_data` value ∈ `[1, n_tokens]` (mirror the existing length/`n_states` checks), `Rcpp::stop` otherwise. Verified REAL (opus) by independent source trace at HEAD `c74ee6e6` — guard confirmed absent, `git`-confirmed unchanged since harness `91918d1a` (tree==HEAD, no in-flight edits). Logic-traced, no repro run; severity set by the T-323 precedent (same internal-boundary OOB → segfault). |
| T-329 | P2 | 13 (Constraints) | Impossible (non-laminar) constraint reaches the kernel unchecked → OOB heap write in `random_constrained_tree` (+ silent constraint-violating tree, TBR freeze) | `R/MaximizeParsimony.R:64-129` (`.PrepareConstraint`), `src/ts_wagner.cpp:984-1161` (`random_constrained_tree`), `src/ts_constraint.cpp:353-360` (`regraft_violates_constraint`) | `.PrepareConstraint` builds one split per constraint character and filters only by split size (`1 ≤ sum < n−1`); it does **not** check laminarity/compatibility, so an *impossible* constraint (two overlapping, non-laminar splits — which no tree can display) passes through to the C++ builder. `random_constrained_tree` then silently best-efforts: tip-ownership assigns each tip to its first/tightest matching split (`:1038-1040`, `break` on first match, `std::sort` tie-break on equal popcount), so a "losing" overlapping split's clade collapses and its constraint is never displayed — no posthoc check or warning (unlike `random_wagner_tree`/`biased_wagner_tree`; cf. T-324). The returned tree violates the (impossible) constraint. Downstream, `regraft_violates_constraint`'s `cn < 0` branch (`:358`) then rejects **every** TBR candidate unconditionally and silently for the rest of the replicate (the undisplayable split's `constraint_node[s]` stays −1 forever) → search freezes, no progress, no diagnostic, wasted budget. Verified REAL (opus verifier): WGN-13-A silent-drop CONFIRMED; WGN-13-B TBR-freeze CONFIRMED but as a **downstream symptom** of A (only reachable from impossible input — a valid/satisfiable constraint keeps `constraint_node[s] ≥ 0` after build), not independent. **Adjacent high-severity OOB hypothesis (WGN-13-C) — CONFIRMED by code-trace (this is the P2 driver).** In `random_constrained_tree`, `:1076-1078` guards the empty-group case (`split_root[i] = -1; continue`) and the root-level collection at `:1093` guards `split_root >= 0` — **but the child-split collection at `:1072` (`items.push_back(split_root[j])`) has NO such guard.** A collapsed split (all owned tips stolen by tighter non-laminar splits → `split_root = -1`) that has a strict-superset parent injects `-1` as a **phantom item** into the parent's `resolve_randomly`, which then (a) does `tree.parent[-1] = nd` (OOB write before the array start) and (b) treats `-1` as a phantom leaf → one extra internal node → `next_internal` reaches `2·n_tip−1`, so `ni = next_internal − n_tip = n_tip−1` writes `tree.left/right[n_internal]` one past the end (`tree.left/right` sized `n_internal = n_tip−1`, `tree.parent` sized `2·n_tip−1`; `init_wagner_state` `:25-32`). Reachable via a ≥3-way-overlap impossible constraint (a collapsed split with a parent needs non-laminar smaller splits to steal all its tips). Same OOB-write class as the confirmed T-323/T-328. (The opus verifier's "invariant proof" — consumption always `n_tip−1` — REFUTED this, but it MISSED the `:1072` unguarded `-1` push; caught on an advisor-prompted re-trace.) Not yet reproduced under ASan (local ASan blocked; a non-ASan run may not visibly crash — heap slack) → a GHA-ASan repro is the definitive-confirmation follow-up. Fix: (a) primary — validate constraint satisfiability/laminarity (pairwise split compatibility) in `.PrepareConstraint` and `stop()` cleanly on an impossible constraint before it reaches the kernel (also pre-empts the impossible-constraint subset of T-324); (b) defensive — add the `split_root[j] >= 0` guard at `ts_wagner.cpp:1072` to mirror `:1093`, so a collapsed split can never inject `-1`. P2 = OOB heap write reachable from public `MaximizeParsimony(constraint=)` on an impossible constraint (currently accepted → crash instead of a clean error). |
| T-330 | P1 | 11 (Collapse) | Zero-length-branch collapse is blind to HSJ/XFORM character support → over-collapses correctly-supported clades AND corrupts the MPT set during enumeration (default `collapse=TRUE` + `inapplicable="hsj"`/`"xform"`) | `src/ts_collapsed.cpp:40-106` (`compute_collapsed_flags`) & `:108-209` (`_aggressive`), `src/ts_tbr.cpp:2374,2419` (enum dedup), `src/ts_rcpp.cpp:1944` (`ts_collapse_pool`) | Both `compute_collapsed_flags` (conservative) and `compute_collapsed_flags_aggressive` decide min-length-0 branches by iterating **only** `ds.blocks[]` (standard + NA blocks); each has a `has_na` special-case (`:49`, `:116-120`) but **neither reads `ds.hierarchy_blocks` (HSJ) or `ds.sankoff_*` (XFORM)**, where those modes store their character contribution (hierarchy chars are zero-weighted out of the standard blocks by `.NonHierarchyWeights`, `R/CharacterHierarchy.R:457-471`, then dropped by `simplify_patterns`). So a clade supported *only* by a hierarchy/Sankoff character looks unsupported (min-length 0) and is contracted. **Two manifestations, both blind:** (1) the default-on final collapse via `ts_collapse_pool` over-collapses result trees — **reproduced end-to-end** (isolated worktree @ HEAD `c74ee6e6`): a 6-tip tree whose `(t3,(t4,t5))` clade is uniquely HSJ-supported (HSJ length 2 vs 3) is returned by `MaximizeParsimony(..., inapplicable="hsj", collapse=TRUE, maxReplicates=1)` with that clade destroyed (`Nnode` 5→3), **identical whether `hsjConfig` is passed or not** (proving the kernel never consults HSJ); (2) the MPT-enumeration dedup at `ts_tbr.cpp:2374`/`:2419` (`compute_collapsed_flags`+`add_collapsed`, active when `collect_pool` set) has **no `scoring_mode` guard** — unlike the incremental-rescore paths gated to EW/IW/XPIWE/PROFILE at `:1348-1352`, `:890-893` — so two HSJ-distinct MPTs collapse to one key and are **wrongly merged → the returned MPT set is corrupted during search**, not just at display. XFORM shares the architecture (source-confirmed via the disjoint `ds.sankoff_*`; not independently reproduced). Default-on since 2026-06-24; nothing in `MaximizeParsimony.R` disables collapse for HSJ/XFORM. **Blast radius: HSJ/XFORM analyses only** (prioritization note, NOT a severity reducer — for those users it is the normal path and the collapse output *is* the deliverable, no downstream recompute to save it; contrast T-306, downgraded because its result stayed correct). Fix: make collapse HSJ/XFORM-aware at **every** call site (`ts_tbr.cpp:1385/1386`, `:2374`/`:2419`, `:2451`/`:2513`/`:2535`, and `ts_collapse_pool`); simplest safe route = no-op the flags (all-zero = no collapse) when `ds.scoring_mode ∈ {HSJ, XFORM}` — "fall back to conservative" (the NA remedy) is **insufficient because the conservative function is equally blind**; a true-collapse fix would fold the HSJ `a(n)/p(n)` DP / Sankoff cost into the min-length-0 test. Verified REAL: source-confirmed (both functions read only `ds.blocks`; enum sites unguarded) + finder end-to-end repro. Repro: `scratchpad/rt11-wt` worktree, `hsj_clean_repro.R` (direct `ts_collapse_pool`), `hsj_clean_repro2.R` (full `MaximizeParsimony`). Needs a GHA regression test across EW/IW/profile/HSJ/XFORM. |
| T-332 | P3 | 11 (Collapse) | `ts_collapse_pool` hangs indefinitely on a non-binary (multifurcating) edge matrix — internal Rcpp boundary lacks input validation | `src/ts_rcpp.cpp:1933-1943` (`ts_collapse_pool` per-tree loop: `init_from_edge` + `reroot_at_tip`) | The internal (`TreeSearch:::`-reachable via `RcppExports.R:222`, not exported) `ts_collapse_pool` assumes each internal node has exactly 2 children; fed an edge matrix with a trifurcating root (constructible via raw `ape::read.tree` + `Preorder`/`RenumberTips`, bypassing `MaximizeParsimony()`'s own `RootTree`/`MakeTreeBinary` normalization at `MaximizeParsimony.R:846-855`), it **hangs indefinitely** (finder killed it at 15s; an equivalent binary tree finishes in ~2ms) — likely an unterminated `while (cur != root)` walk in `reroot_at_tip`/`build_postorder` when the 2-children invariant breaks. **Not reachable via the documented public API** (result/best trees come from the engine's always-binary pool; finder found no production path feeding a non-binary matrix) → like T-323/T-328 an **internal-boundary robustness gap**, P3 — but a hang is worse than a clean error. Fix: validate binary-ness at the Rcpp boundary and `Rcpp::stop` otherwise (mirror the T-323/T-328 guard class). Verified REAL (finder repro `scratchpad/hang_narrow3.R`; reachability `TreeSearch:::`-only). |
| T-334 | P3 | 2 (Topology) | ~~Aggressive collapse-neighbourhood criterion silently reverts to conservative after the first accepted move~~ **FIXED** | `src/ts_tbr.cpp:2859, 2991` | Confirmed unintentional: the strategy briefing (`dev/plans/2026-06-22-collapse-aggressive-strategy-briefing.md`) documents testing/results only and contains no "explore aggressively once, then settle" design rationale; the code comments at both sites ("Recompute collapsed regions after the accepted move", "legacy reroot") are purely mechanical bookkeeping with no stated intent to revert criteria. Fixed by branching on `collapse_aggr` at both sites, mirroring the existing pattern at `:1539-1540`/`:2967-2968`. Rebuilt clean (`build-fast.R`, 9.7s). Closed. |
| T-335 | P3 | 3 (Ratchet/Perturbation) | `TS_SECT_COLREDUCE` per-sector reducer leaves per-pattern arrays stale — inert under the EW-only gate, would be a live wrong-score + OOB bug if the gate is ever loosened toward IW/weighted | `src/ts_sector.cpp:315-317` (EW-only gate), `:407-411` (stale-array copy in `reduce_sector_columns_ew`) | The new opt-in per-sector column-axis reducer (`830b8cc3`, `TS_SECT_COLREDUCE`, **default-OFF**) rewrites `pattern_index` to identity (`0..nchar-1`) but leaves `n_patterns` / `min_steps` / `pattern_freq` / `precomputed_steps` at **full-dataset size and content** (unmodified copy), and additionally leaves the newer per-block `plane_state` field **zero-initialized** (`CharBlock cb{}` at ts_sector.cpp:362 never assigns it, even though the reducer repacks planes into a new layout — added 2026-07-24 area-5 round as PACK-COLRED-01, verified REAL-but-inert by haiku; its only consumer `ts_na_debug_char` at ts_rcpp.cpp:494 is NA-only and builds a fresh full dataset, never a reduced one, so it is never read on this EW path). **Inert today** (verified): a hard gate at `:315-317` restricts the reducer to pure EW — `scoring_mode==EW && all_weight_one && no block has upweight_mask/has_inapplicable` — and the EW scorers actually invoked on the reduced sector (`fitch_score`→`fitch_downpass`/`fitch_uppass`, `fitch_indirect_length_cached`, `ts_fitch_combine`, `fitch_score_ew`+`ew_offset`) read only block structure (`active_mask`/`n_states`/`block_word_offset`/`weight`/`upweight_mask`), never those four per-pattern arrays. So the stale arrays are never read on this path. **Why filed (tripwire, not a live bug):** if the EW gate is ever loosened toward IW/weighted, or the reducer is reused for a mode that reads `pattern_freq`/`min_steps`, the identity `pattern_index` + stale `pattern_freq`/`min_steps` would give **wrong weighted scores** AND a **possible OOB read** (`min_steps`/`precomputed_steps` stay sized for the OLD `n_patterns`, which can exceed the reduced char count). Filed P3 because a concurrent session is actively extending `ts_sector` (mission-b speed work) — this is exactly the gate a perf change might widen. The firewall (full-dataset `score_tree` on splice-back accept, see area-3 round 2026-07-24) still catches the *returned* score even if this breaks; direct impact would be search-quality/OOB inside a sector. Fix if the reducer is generalised: rebuild `n_patterns`/`min_steps`/`pattern_freq`/`precomputed_steps`/`plane_state` to the reduced character set, not just `pattern_index`. Verified REAL (haiku): stale-array copy + EW-only gate + scorer array-independence all confirmed; inert under current gates. |
| T-336 | P1 | 4 (Parallelism/RNG) | ~~Shared `ConstraintData` raced across parallel `Resample()` workers → data race (UB) + wrong-tree constraint gating + non-reproducibility~~ **FIXED (f951df5a)** | `src/ts_parallel.cpp:637-661` (parallel_resample worker lambda), `src/ts_rcpp.cpp:2299-2324` (`ts_parallel_resample` builds one `cd`), cf. `:159-164` (parallel_driven per-worker copy) | `ts_parallel_resample` builds ONE `ConstraintData cd` and passes the same pointer to `parallel_resample`, whose worker lambda captures it by `[&]` and hands the **identical** `cd` to every worker's `resample_search`→`driven_search(pool,ds,params,cd)` (ts_resample.cpp:112). During each replicate's search, workers **concurrently WRITE** the shared (non-`mutable`) members `cd->constraint_node`/`dfs_entry`/`dfs_exit`/`clip_zones`/`clip_tip_mask` via `map_constraint_nodes`/`update_constraint`/`classify_clip_constraints`/`impose_constraint` (ts_constraint.cpp:166,175,205,209; also ts_wagner/ts_tbr/ts_ratchet/ts_driven mutate `*cd`) with **tree-specific** node indices, then read them back to gate `fused_ok` acceptance (ts_driven.cpp:1114-1118,1127-1128). By contrast `parallel_driven`'s `worker_thread` deliberately makes a per-worker copy `ConstraintData cd_local = *ctx.cd_prototype` (ts_parallel.cpp:159-164) — the resample path has **no** such copy, proving the omission is a real divergence, not a deliberate no-mutation optimisation. Unsynchronized concurrent read/write to shared vectors = data race (UB) + constraint validated against another worker's tree mapping + non-reproducibility. Reachable via public `Resample(dataset, constraint=…, nReplicates>1, nThreads>1)` (R/Resample.R:363-368). **NOT bounded by the full-rescore firewall** (that re-scores the final tree; it cannot un-corrupt a heap or un-race a data race — the areas-2/3 "it's just reach" reasoning does not transfer to concurrency). Adjacent threads verifier-cleared: `.ResampleHierarchy` is NOT a second instance (serial R for-loop, `nThreads=1L`, each call builds own `cd` — REFUTED); the `posthoc_data` single-writer compounding is REFUTED (`posthoc_data` read-only on this path — `violates_constraint_posthoc` scores via `fitch_score` against a thread-local ctree + const ds, never touches the `mutable` evs_false_cache). Fix: mirror parallel_driven — inside the resample worker lambda, give each worker its own `ConstraintData cd_local = *cd;` and pass `&cd_local`. Verified REAL (opus finder + opus peer verifier, both independently tracing all five links at HEAD 31b06276). **FIXED same-turn 2026-07-24 (f951df5a)** — per-worker `cd_local = *cd;` copy added to the resample worker lambda (mirrors parallel_driven; one copy/worker suffices as searches run sequentially within a worker; null/inactive cd passes through unchanged); build clean (build-fast 6.6s) + functional smoke (constrained & unconstrained parallel `Resample`, nThreads 2-3) passes. Thread-race validation deferred to GHA-ASan/TSan (local ASan blocked on MinGW). (Kept here until merged.) |
| T-337 | P2 | 4 (Parallelism/RNG) | **FIXED (b1482997, 2026-07-27) — awaiting `tidy` archive.** `build_dataset`→`Rf_error` (R API) fires on a worker thread on the parallel `Resample()` path for >32-state input → cross-thread longjmp (UB/crash); same class as T-309 | `src/ts_data.cpp:32` (`Rf_error` when `n_states>MAX_STATES=32`), reached from `resample_search` (ts_resample.cpp:96) inside the `parallel_resample` worker lambda (ts_parallel.cpp:650); cf. the cited-but-preempted `ts_resample.cpp:44,50` | `Rf_error()` longjmps to R's main-thread context; on the parallel resample path this runs on a **worker thread**, so the longjmp crosses threads = UB/crash (same failure class as T-309, whose fix converted `unif_rand`→`thread_safe_unif` but left the error-guards). Verification precision: the two ORIGINALLY-cited guards at ts_resample.cpp:44 (negative char weight) and :50 (`sum(weights)>INT_MAX`) are **pre-empted on the main thread** by `.ScaleWeight` (R/Resample.R:301 → fractional-weights.R:28,53) → not reachable via `Resample()`. The genuinely reachable trigger is the named companion: `build_dataset`→`Rf_error` at ts_data.cpp:32 fires when `n_states = contrast.ncol() > 32`, and there is **no n_states cap anywhere on the main-thread resample path** (the only R `nStates>32L` check, recode_hierarchy.R:86, is XFORM-only → routes to serial `.ResampleHierarchy`). Static divergence: a >32-state dataset with `nThreads=1` errors cleanly on the main thread; with `nThreads>1` the identical code runs on a worker → cross-thread longjmp. P2 (crash on a degenerate-but-plausible >32-state input under parallel resample; not a wrong result on normal input). Fix: validate `n_states ≤ MAX_STATES` (and the weight conditions) on the MAIN thread in `ts_parallel_resample` before spawning workers, erroring cleanly there. Verified REAL (opus peer verifier); reachable trigger corrected from the cited weight-guards to the n_states>32 companion. **Fix landed (b1482997):** hoisted the n_states check to the main thread in `ts_parallel_resample` (`src/ts_rcpp.cpp`), plus two more of the same class found while sweeping `resample_search`'s call graph for other `Rf_error` reachable from a worker: `n_tip < 3` (`ts_wagner.cpp:17`, reached via `driven_search`→Wagner construction) and the negative-weight/`INT_MAX`-overflow guards `ts_resample.cpp:44,50` — the latter two are pre-empted on the public `Resample()` path by `.ScaleWeight` as already noted, but not on a direct internal call to `ts_parallel_resample`, so hoisted as defense-in-depth on the internal entry point itself. Regression test: `tests/testthat/test-ts-resample.R` ("Resample with > 32 states errors cleanly on the main thread"). |
| T-338 | P3 | 4 (Parallelism/RNG) | Three latent worker-thread tripwires in the parallel resample path — all INERT today (getenv thread-safety / Rprintf-verbosity / `TS_AUDIT_PROBE` statics) | `src/ts_parallel.cpp:59` + `src/ts_driven.cpp:670,760,1052,1260` (getenv); `src/ts_driven.cpp:1289-1294,1321-1324` (Rprintf); `src/ts_fitch.cpp` g_x4_* + `src/ts_sector.cpp:413-418` (audit statics) | Three REAL-but-inert worker-thread hazards, filed as anti-dup tripwires because the concurrent mission-b work on this path could flip any live. **(A4-OP-03, getenv)** `getenv()` read on workers (ts_parallel.cpp:59 under mutex; ts_driven.cpp:670/760/1052/1260 via driven_search on the resample-worker path). Concurrent getenv reads are safe absent a concurrent `setenv`/`putenv`, and there is NONE in `src/` (grep-confirmed) → INERT; escalates only if C-level env mutation is ever added. **(A4-OP-04, Rprintf)** new verbosity≥1 terminal-fuse `Rprintf` at ts_driven.cpp:1289-1294/1321-1324 runs inside driven_search (resample-worker-reachable), but `verbosity` defaults 0 (ts_driven.h:149) and `ts_parallel_resample` never sets it (ts_rcpp.cpp:2307-2312) → INERT; becomes a live R-API-on-worker bug if verbosity is ever wired into the parallel resample path. **(A4-OP-05, audit statics)** `TS_AUDIT_PROBE` file-static counters (ts_fitch.cpp g_x4_waste/g_x4_total/g_x4_calls + fprintf; ts_sector.cpp:413-418 `static bool announced` + Rprintf) race and print from workers, but are entirely `#ifdef TS_AUDIT_PROBE` and that macro is NOT default (no CI workflow defines it; requires explicit `-DTS_AUDIT_PROBE`) → INERT in all production/CI builds; keep audit-probe profiling runs serial. All three Verified REAL-but-INERT (haiku). Fix when relevant: main-thread getenv hoist / never wire verbosity to the parallel resample path / keep audit builds serial. |
| T-339 | P2 | 6 (R↔C++ interface) | **FIXED (b1482997, 2026-07-27) — awaiting `tidy` archive.** `nThreads = 0` silently runs serial instead of the documented "auto-detect cores" behaviour, on BOTH `MaximizeParsimony()` and `Resample()` | `src/ts_rcpp.cpp:1843` (`ts_driven_search`), `:2317` (`ts_parallel_resample`) — dispatch gate; `src/ts_parallel.cpp:283-288`, `:618-622` (`n_threads<=0` auto-detect, dead code behind the gate) | The parallel dispatch gate at both Rcpp entry points is `if (nThreads > 1 ...)`, so `nThreads = 0` never reaches the auto-detect logic (`if (n_threads <= 0) n_threads = hardware_concurrency()`), which lives one layer deeper *inside* `parallel_driven_search`/`parallel_resample` — it falls straight through to the single-threaded path instead. `R/MaximizeParsimony.R:549-554` (and `R/Resample.R:160-161`, "as for `MaximizeParsimony()`") documents `0` as "Auto-detect: use one fewer thread than the number of CPU cores." No error, no warning — a user who sets `nThreads=0` expecting parallel search silently gets serial. **Two-layer bug:** even if the gate were fixed to `!= 1`, the auto-detect implementation itself (`ts_parallel.cpp:284-288`/`619-622`) uses the full `hardware_concurrency()` (floor 2, capped at `max_replicates`) and never subtracts one — a second, currently-dead, doc/impl mismatch that the gate fix would expose. Reachable via public `MaximizeParsimony(nThreads=0)` and `Resample(nThreads=0, nReplicates>1)` — both pass the value through unnormalized. Fails safe (correct trees, just no parallelism), so functional/performance regression on a documented knob, not a correctness bug — directly relevant to the wall-clock-to-optimum mission and to any Hamilton-style HPC invocation that relies on `nThreads=0` auto-detect. Fix: change both dispatch gates to `!= 1` (or `< 1 || > 1`), then decide/implement the intended auto-detect semantics (cores-minus-one vs raw `hardware_concurrency()`) and update whichever side (doc or code) is wrong. Verified REAL (opus finder + opus peer verifier, both tracing all cited lines independently; found by two independently-run finders, opus and fable, at the same tier on the same fresh scope — no overlap with fable's findings). **Fix landed (b1482997):** both gates changed to `!= 1`; auto-detect now does `hardware_concurrency() - 1`, floored at 1, matching the docs. Also caught and fixed a third, same-class stale doc: `R/MaximizeParsimony.R`'s `candidates_evaluated` field said "`0` when `nThreads > 1`", which the gate fix makes wrong for `nThreads = 0` too — reworded to "`0` for any parallel search (`nThreads != 1`)". Regression tests use per-replicate seeding determinism (parallel results are thread-count-independent, since each replicate is seeded from a main-thread-generated array) rather than wall-clock timing, so `nThreads = 0` and `nThreads = 2` are asserted byte-identical while `nThreads = 1` (which draws from R's live RNG stream) diverges — see `test-ts-parallel.R` and `test-ts-resample.R`. |
| T-340 | P2 | 6 (R↔C++ interface) | `concavity` argument accepted as a character value with no normalization → silently wrong IW scores (numeric-string case) or silent EW fallback (case-variant "profile") | `R/MaximizeParsimony.R:830-831,888,1015,1040-1042` (concavity dispatch/coercion), `src/ts_data.cpp:453,469,472-473` (min_steps population + isfinite/NaN routing), `src/ts_fitch.cpp:1320,1333` (`compute_iw`/`precompute_iw_delta` read `ds.min_steps` with no recompute), `R/MaximizeParsimony.R:1185-1187` (`.UseProfile()`, defined but not used at the search entry) | `MaximizeParsimony()` never normalizes a character-valued `concavity` before dispatch, and the *search* entry point disagrees with the *scoring* entry point on how to recognize "profile" mode. **Symptom A (fully silent, worse):** `concavity = "10"` (numeric-coercible string) — `is.finite("10")` is `FALSE` in R, so R skips `MinimumLength()` (never populates `min_steps`) and skips the `concavity<=0` EW-conversion guard; but `as.double("10")` cleanly produces `10.0` with **no warning**, so C++ receives `concavity=10.0`, enters IW mode (`isfinite(10)` true at ts_data.cpp:469), yet `ds.min_steps` stays all-zero (the line that would populate it never ran) — `compute_iw`/`precompute_iw_delta` read `ds.min_steps` directly with no fallback recompute, so homoplasy goes uncorrected: **silently wrong IW scores, no error or warning anywhere.** **Symptom B (self-warning, milder):** the live profile-mode check at `MaximizeParsimony.R:831` is `identical(concavity, "profile")` — case-sensitive, exact-match only — while the lenient matcher `.UseProfile()` (`pmatch(tolower(...))`) exists and IS used at the *scoring* entry points (`tree_length.R:113/169/243`, `PolEscapa.R:70`) but not here. `concavity = "Profile"` or `"prof"` fails the strict search-entry check, falls through `as.double()` to `NA` (with an R coercion warning) → `NaN` in C++ → routes to EW instead of profile mode — so a user's search and their post-hoc scoring would disagree on which mode ran. **One root cause, two symptoms, one fix:** add a single `concavity` normalization step at the top of `MaximizeParsimony()` — route the profile test through the already-existing `.UseProfile()` (closes symptom B) and `as.numeric()` with an NA/positivity guard for everything else (closes symptom A) — reachable via public `MaximizeParsimony(concavity=...)`. Verified REAL (fable finder + opus peer verifier, both tracing all cited lines independently; opus verifier additionally confirmed the shared root cause and recommended the one-fix framing above). |
| T-341 | P3 | 6 (R↔C++ interface) | `maxReplicates < 1` unchecked in R → search silently runs zero replicates and returns the random starting tree | `R/MaximizeParsimony.R:1029-1030` (unchecked `as.integer(maxReplicates)`), `:1118-1119` (empty-pool fallback to `treeTpl`), `src/ts_driven.cpp:1355` / `src/ts_rcpp.cpp:1855,1887` (`best_score=-1.0` on an empty pool) | `maxReplicates` passes straight through with no `>= 1` check. `maxReplicates = 0` runs the search loop zero times, leaves the pool empty, `best_score` stays `-1.0`, and R's own fallback (`outTrees <- list(treeTpl)`) silently returns the random NJ/user-supplied starting tree tagged with `attr(,"score") = -1` — no error, no warning. The `-1` score is a self-flagging sentinel for anyone who checks it, which is why this is P3 rather than P2, but a caller who doesn't inspect the score gets a meaningless "result" with no indication anything went wrong. Fix: `stopifnot(maxReplicates >= 1)`-style guard in the R layer. Verified REAL (haiku verifier, traced all cited lines). |
| T-342 | P3 | 6 (R↔C++ interface) | Fractional-weight datasets get a ~1260×-inflated "recommended replicates" warning (cosmetic only) | `R/MaximizeParsimony.R:954-955` (`nChars <- sum(weight)`, `minReps <- ceiling(nTip*nChars/5000)`), `R/fractional-weights.R` (`.ScaleWeight`, default scale ~1260×) | `weight` at this point is the *scaled integer* weight from `.ScaleWeight`, not the original character weight — a no-op for integer-weight datasets but a ~1260× multiplier for fractional-weight ones. The resulting `minReps` recommendation in the printed warning is inflated by the same factor for fractional-weight data. No behavioral effect (search runs identically; this only feeds a user-facing message), so filed rather than fixed inline pending a decision on whether `nChars` should be computed pre- or post-scaling. Verified REAL (haiku verifier, traced `.ScaleWeight` behavior on fractional vs integer weights). |
| T-343 | P3 | 6 (R↔C++ interface) | Several documented `SearchControl()` parameter ranges are unvalidated in R, unlike sibling parameters that already guard | `R/SearchControl.R:378-390` (existing guards: `xssPartitions`/`cssPartitions`/`poolMaxSize >= 1`, `stallEscalateFactor >= 1`); undocumented-range fields: `enumTimeFraction` (doc: `[0,0.5]`, `SearchControl.R:32`), `nniPerturbFraction` (doc: `[0,1]`, `:163`), `ratchetPerturbProb` (doc: `[0,1]`, `:244`), `sectorMinSize <= sectorMaxSize` | These four documented constraints have no corresponding guard alongside the four that already exist just above them — an inconsistency, not a new pattern. Out-of-range values (e.g. a negative fraction, or `sectorMinSize > sectorMaxSize`) pass straight through to C++ unchecked and produce silently degenerate search behavior (not a crash — no fixed-array indexing on these fields). Fix: extend the existing `SearchControl.R:378-390` guard block to cover these four, matching the established pattern. Left as a design decision (reject vs clamp out-of-range input) rather than fixed inline. Verified REAL (haiku verifier, confirmed each field documented but unchecked). |
| T-344 | P3 | 6 (R↔C++ interface) | `unpack_xform` reads `tip_states[t]` with no length guard — internal-boundary-only, same class as T-323/T-328/T-332 | `src/ts_rcpp.cpp:1788-1791` (no bounds check on `tip_states`, contrast `make_dataset`'s guarded read) | Missing length guard is real, but the only path the fable finder or its verifier could find into `unpack_xform`/`ts_driven_search` with a raw `xformConfig` is a direct internal `.Call`, not the documented public API — `MaximizeParsimony()`'s HSJ/XFORM path builds `tip_states` internally via `RecodeHierarchy` (`R/recode_hierarchy.R:122`), which always produces the correct length. Same "internal-boundary robustness gap" class as T-323/T-328/T-332 — a hardening item, not a reachable bug today. Fix: mirror `make_dataset`'s bounds check. Verified REAL-but-not-publicly-reachable (haiku verifier). |
| T-354 | P2 | 7 (Shiny) | **EasyTrees "R script" download reconstructs a DIFFERENT tree subset than the user saw — stride sampling substituted for `WideSample()`'s diversity solver** | Emitter `inst/Parsimony/server/logging.R:155-161` (`BeginLogP`, feeds `plotLog` → `savePlotZip`); correct sibling at `inst/Parsimony/server/mod_data.R:198-206` (`cmdLogFile`, "Save log") | Two logs record the SAME thinning step differently. `mod_data.R:202-206` correctly logs `trees <- WideSample(allTrees[a:b], n)`; `BeginLogP()` instead emits `trees <- trees[unique(as.integer(seq.int(a, b, length.out = n)))]` — naive evenly-spaced striding, where `WideSample()` (`R/WideSample.R`) is a Max-Min Diversity Problem solver (FarFirst/DropAdd/exact tiers). **Confirmed by a COMMITTED ARTIFACT, not by tracing:** `inst/Parsimony/tests/testthat/_snaps/Distribution/Distribution-001-ClusterCons.zip:50` is a currently-passing snapshot of the real `savePlotZip` output (test mode writes the script as plain text with a `.zip` extension, `mod_downloads.R:78-96`) and literally contains `trees <- trees[unique(as.integer(seq.int(1, 125, length.out = 48)))]` for a run where the app executed `WideSample(allTrees[1:125], 48)`. **Measured divergence** (Sun2018, 125 trees, vs the deterministic `effort = 1` FarFirst tier — an honest lower bound, since the app auto-selects the exact tier here): overlap 19 of 48; min pairwise `ClusteringInfoDistance` 0 (stride) vs 8.9149 (WideSample) — i.e. the stride subset contains duplicate topologies, precisely what `WideSample()` exists to prevent. **Not display-only:** the snapshot's own downstream lines run `ClusteringInfoDistance`, KMeansPP / `cluster::pam` / `protoclust` with `kSil`/`pamSil`/`hSil` silhouette selection, and `ConsensusWithout()` — so the wrong 48 trees change reported numeric results (silhouette coefficients, chosen *k*) and reported consensus topologies, defeating the feature's stated purpose (`logging.R:78-79`, `:112-113`: "to facilitate the reproduction of ... the interactive Shiny session"). **Default path:** `global.R:96` `aFewTrees <- 48L` and `mod_data.R:256` `UpdateNTree(min(max(input$nTree, aFewTrees), nTrees))` put any tree set >48 on this branch automatically. Gate `!identical(r$trees, r$allTrees)` (`logging.R:155`) is true exactly on the `WideSample()` branch; the one benign corner is `n >= length(rangedTrees)`, where `WideSample()` returns its input unchanged and `seq.int` collapses to `a:b` (correct there) — so the wrong-algorithm case is exactly the genuine-thinning case. `a`/`b`/`n` are the same state fields in both logs; no index-base mismatch. Fix: mirror `mod_data.R:202-206` in the `BeginLogP` emitter — **and regenerate `Distribution-001-ClusterCons.zip`**, the only snapshot carrying the line (`test-Distribution.R:20` sets `nTree = 125`, moving later snapshots onto the identity branch). See also T-360 (the correct sibling line still lacks a seed). Verified REAL (opus peer verifier), severity raised medium-high → P2. |
| T-355 | P3 | 7 (Shiny) | **`r$trees` mutated on a path that never updates `r$treeHash`, breaking the documented cache-key invariant (latent — no reachable wrong output today)** | `inst/Parsimony/server/mod_treespace.R:75-78` (`FirstHit <- reactive({ r$trees <- WhenFirstHit(r$trees); ... })`) | `app_state.R:34` documents `r$treeHash` as "`rlang::hash()` of trees (change trigger)". Writer list confirmed complete by grep: `r$treeHash <-` appears at exactly `mod_data.R:186`, `:210`, `:637`, while `r$trees <-` appears at those three plus `mod_treespace.R:76` — the treespace write is the one path with no paired hash update, so ~9 caches (`Rogues`, `concordance`, `distances`, `clusterings`, `mapping`, `mstEnds`, `output$treePlot`, `output$pcQuality`, `RCode`) key on a hash that no longer matches their input. **The write genuinely fires:** shiny 1.13.0's `ReactiveValues$set` short-circuits on `identical()` (`.dedupe`, confirmed by deparsing `shiny:::ReactiveValues$public_methods$set`), but plain `[` on a `multiPhylo` drops the `firstHit` attribute while preserving names (verified), so `mod_data.R:198`'s `r$allTrees[a:b]` strips it, `WhenFirstHit()` re-adds it, the value differs, and the write lands with `treeHash` stale. **No reachable wrong answer:** `R/WhenFirstHit.R` only does `attr(trees, "firstHit") <- table(...)` — no reorder/subset/relabel — and the three consumers spot-checked (`mapping()` `mod_treespace.R:278`, `mstEnds()` `:296`, `output$treePlot`/`RCode` `mod_consensus.R:973-1062`) compute from topology only; the `"space"` cache key additionally includes `TreeCols()`/`ts_spaceCol()`, so switching colour mode to `firstHit` changes the key anyway and the stale-hash cache **hit** still returns a correct value. **Reachability is narrow:** the mutation needs every tree name to match `(seed|start|ratch\d+|final)_\d+`, and no in-package producer of those names or of the `firstHit` attribute was found in `R/` or `src/` — so it fires only for externally-named tree files (legacy saved output). `test-Distribution.R:58` exercises `spaceCol = "firstHit"` on Sun2018, whose names don't match. Filed as a durable invariant break: nothing re-derives the hash, so any future change making `WhenFirstHit()` (or a copy of this pattern) alter the trees themselves becomes a live wrong-cache bug silently. **Do not re-file as a third finding:** this mutation also flips `!identical(r$trees, r$allTrees)` at `logging.R:155` on the otherwise-identity branch, but the line then emitted is `seq.int(1, N, length.out = N)` = `1:N`, a harmless no-op. Verified REAL (opus peer verifier), severity lowered medium → P3. |
| T-356 | P3 | 7 (Shiny) | **Two browser tabs on one app instance silently overwrite each other's cached download files (deterministic names in a process-wide tempdir)** | `inst/Parsimony/server/logging.R:205-213` (`DataFileName`/`ExcelFileName`/`TreeFileName`) and `:221-231` (`CacheInput`/`StashTrees`); consumed at `inst/Parsimony/server/mod_downloads.R:60-68,120-125` | Names are built deterministically (`"dataFile-01.txt"`, `"treeFile-02.txt"`, …) from **per-session** counters (`r$dataFiles`/`r$treeFiles`/`r$excelFiles`, reset to 0 each session at `logging.R:199-201`) but written into `tempdir()`, which is per-R-**process**. `EasyTrees()` (`R/MaximizeParsimony.R:1279-1291`) launches one `shiny::runApp()` process, so opening the running instance in two tabs — a plausible way to compare two datasets — has both sessions' first upload reach counter `1` and write `tempdir()/treeFile-01.txt`. `zip::zip(mode = "cherry-pick")` (`mod_downloads.R:120-125`) then bundles whichever tab wrote last: no error, no warning, wrong content in one tab's download. Inconsistent with `cmdLogFile <- tempfile("TreeSearch-", fileext = ".R")` (`logging.R:5`), which already uses the globally-unique pattern — suggesting oversight rather than design. Fix: `tempfile()`, or namespace the counter by `session$token`. Verified REAL (haiku). |
| T-357 | P3 | 7 (Shiny) | **Orphaned UI output: `instabLegend2` is declared in the UI but never bound server-side, so the legend panel renders empty in two views** | `inst/Parsimony/ui.R:93` (`hidden(tags$div(id = "clusLegend", htmlOutput("instabLegend2", inline = TRUE)))`) | Shown whenever `plotFormat` is `"clus"` or `"space"` (via `ShowConfigs()` in `server.R`). A whole-repo grep finds exactly one reference to `instabLegend2` — this line — and zero `output$instabLegend2 <- ...` declarations anywhere, including no `renderUI`-constructed or `ns()`-namespaced variant inside a module. The sibling static `"instabLegend"` gradient legend IS present (`mod_consensus.R:99`), which is the basis for reading this as an oversight: likely leftover from the `mod_consensus.R` header comment's claim to have absorbed "residual clustering.R ... from events.R" — the binding didn't make the move. Net effect: the panel is empty for the entire lifetime of the "Cluster consensus trees" and "Tree space" views. Verified REAL (haiku). |
| T-358 | P3 (perf) | 7 (Shiny) | **Cluster-threshold slider re-runs the entire multi-method clustering sweep, though the threshold only picks among already-computed silhouette scores** | `inst/Parsimony/server/mod_clustering.R:91-163` (`clusterings <- bindCache(reactive({...}), r$treeHash, silThreshold(), distMeth())`) | The reactive body computes K-means++, PAM and hierarchical-minimax clusterings for every *k* in 2..15 — three full sweeps, each with a `cluster::silhouette()` pass (lines 108-137) — and only afterwards reads `silThreshold()` to choose a winner (`which.max(c(silThreshold(), pamSil, hSil, kSil))`, line ~140). Verifier confirmed `silThreshold()` feeds **none** of the candidate computations, only the final selection, and that it sits in the `bindCache` key list (`:163`) — so every settle of the debounced `clThresh` slider (50 ms) is a cache miss that redoes the whole expensive computation. Fix: hoist the sweep into its own reactive keyed on `r$treeHash`/`distMeth()` only, and select against `silThreshold()` downstream. Verified REAL (haiku); assessed by reading, not benchmarked. |
| T-359 | P3 | 7 (Shiny) | **Tree-space mapping-quality `future_promise()` has no session-end cancellation — the same lifecycle gap T-311 fixed for the search worker** | `inst/Parsimony/server/mod_treespace.R:738-753` (`future_promise(TreeDist::MappingQuality(...))` behind `output$pcQuality`) | The search and profile-prep workers are explicitly signalled on disconnect via `session$onSessionEnded(...)` at `mod_search.R:594-602` (commented "T-311"); a grep of all of `inst/Parsimony/server/` for `onStop`/`onSessionEnded` returns that hit and no other, so this future has no equivalent registration and runs to completion after the client is gone. Impact is far below T-311's (one distance/quality computation, not an up-to-60-minute search), hence P3. Note when fixing: unlike the search worker, which polls a cancel file, `MappingQuality` offers no interruption point — the available remedies are bounding when it is launched or accepting the orphan, not signalling it mid-flight. Verified REAL (haiku). |
| T-360 | P3 | 7 (Shiny) | **"Save log" download's `WideSample()` line has no captured seed, so the correct algorithm can still reproduce a different subset** | `inst/Parsimony/server/mod_data.R:202-206` | Even the *correct* logged line `trees <- WideSample(allTrees[a:b], n)` (the sibling that T-354's emitter gets wrong) is emitted without an accompanying `set.seed()`. Per `R/WideSample.R:29-39` the exact/Grasp tiers draw on the session RNG to break ties between equally-optimal subsets, and the app reaches the exact tier by default when `highs` is installed and the tree count is under `exactCeiling`. So re-running the downloaded script may select a different tie-optimal subset than the user saw — a weaker version of T-354's defect surviving in the log that is otherwise right. Verified REAL (haiku); confirmed independently by the opus verifier while checking T-354. Fix alongside T-354. |
| T-361 | P3 | 7 (Shiny) | **CLOSED (Round 2026-07-27) — verified inert; kept as an anti-duplication record, not open work.** `EnC(character(0))` emits the literal `c()` into generated reproduction code, which evaluates to `NULL` rather than an empty vector | `inst/Parsimony/global.R:411-417` (`EnC`), reached unguarded at `inst/Parsimony/server/mod_consensus.R:857` and `:924` (`EnC(dropped)` with nothing dropped) | `EnC(character(0))` → length 0 → the "multi" branch → `sapply(character(0), Enquote)` → `paste(list(), collapse = ", ")` → `"c()"`, so a downloaded script contains e.g. `tip = c(),` binding `NULL` instead of `character(0)`. **Assessed harmless in every currently-reachable case** — verifier confirmed all consumers (`ConsensusWithout`, etc.) branch on `length(x) == 0`, true for both — so it is **closed on filing** and retained only so a future finder does not re-derive the same conclusion. Do not open work against it; if `EnC`'s consumers ever stop branching on `length(x) == 0`, reopen. Related dead end already ruled out: `EnC(a, b)` with 2+ positional arguments does error, but a grep of every call site in `inst/Parsimony/` (tests excluded) found all are single-argument, so the crash is unreachable. Verified REAL (haiku), impact harmless. |
| T-353 | P1 | directed (warm-start pool) | **REGRESSION INTRODUCED BY THIS DIFF: a `multiPhylo` pool whose tree 1 is fine but whose tree _k_ (k ≥ 2) is unrooted now SEGFAULTS, where pre-diff it ran cleanly** | `R/MaximizeParsimony.R:906-908` (`startTrees <- lapply(seq_along(tree), function(i) tree[[i]])`) feeding the per-tree normalization `lapply` at `:949-973` | **Confirmed by running** (SIGSEGV, exit 139): `good <- Preorder(RandomTree(ds, root = TRUE)); pool <- structure(list(good, ape::unroot(good)), class = "multiPhylo"); MaximizeParsimony(ds, tree = pool, maxReplicates = 2L, targetHits = 99L, verbosity = 0L)`. **Why this is new, not pre-existing:** the removed line was `tree <- tree[[1L]]` — a `multiPhylo`'s trees 2..n were discarded *before normalization ever saw them*, so only tree 1 was ever passed to `MakeTreeBinary`. The diff replaces that with `lapply(startTrees, ...)`, which runs the full binarise/reroot normalization on **every** tree in the pool. Any pool member that trips the T-345 mechanism therefore now crashes a call that previously succeeded by silently ignoring it. This is establishable from the R diff alone — no rebuild needed. Shares T-345's mechanism (`MakeTreeBinary` returns a malformed object on unrooted input → `RootTree` segfaults before the `stop()` guard); see T-345 for the full trace. **Blast radius is wider than it looks:** the diff's whole purpose is to accept externally-supplied tree pools, and users assembling a pool by hand (`c()`, `list()`, trees round-tripped through `ape::unroot`/`read.tree`) can easily include one unrooted member. Fix: whatever fixes T-345, plus per-tree error context (the current `stop("Could not make \`tree\` binary.")` does not say which pool member failed). Orchestrator-reproduced; opus peer verification of the shared mechanism in flight at filing. |
| T-345 | P1 | directed (warm-start pool round; code is area 6 / `R/MaximizeParsimony.R`) | **`MaximizeParsimony(tree = <unrooted bifurcating phylo>)` SEGFAULTS from the public API — the single-tree case is PRE-EXISTING (see T-353 for the pool case, which this diff DID introduce)** | Call site `R/MaximizeParsimony.R:949-960` (the `MakeTreeBinary` → `RootTree(tr, 1L)` → `stop()` block); actual defect is inside `TreeTools::MakeTreeBinary` / `TreeTools::RootTree`, triggered by a stale `order` attribute from `ape::unroot` | Repro (SIGSEGV, exit 139, reproduced twice, single-tree AND pool paths): `ds <- inapplicable.phyData[["Vinther2008"]]; set.seed(21); tree <- ape::unroot(Preorder(RandomTree(ds, root = TRUE))); MaximizeParsimony(ds, tree = tree, maxReplicates = 1L, targetHits = 1L, verbosity = 0L)`. Mechanism traced step-by-step: an unrooted binary tree on n tips has `2n-3` edges and `Nnode = n-2`, so `nrow(edge) != 2*Nnode` and the repair branch is entered; `TreeTools::MakeTreeBinary()` then returns a MALFORMED object — measured `Nnode = 22, nEdge = 47` for 23 tips, where a rooted binary tree must have 44 (`ape::checkValidPhylo()` fails on it); the fallback `RootTree(tr, 1L)` is called on that corrupted object and segfaults **before** the `stop("Could not make \`tree\` binary.")` guard can fire. Contributing factor: `ape::unroot()` leaves a STALE `attr(tree, "order") == "preorder"` on a tree whose edges are no longer in preorder, so `TreeTools::Preorder()` is a no-op; stripping the attribute and genuinely re-ordering converts the segfault into a catchable R error (`NodeOrder.matrix`: "argument must be coercible to non-negative integer"). So the stale attribute turns a clean error into memory corruption. Input is within the documented contract (roxygen says "A bifurcating tree"; an unrooted degree-3 tree is bifurcating, and the code explicitly attempts to repair non-binary input). The diff moved this block into a per-tree `lapply` **unchanged**, so the SINGLE-TREE crash is pre-existing — but see **T-353**: applying it to every pool member is new, and turns a previously-working call into a crash. **Two fixes, two places:** (1) TreeSearch should strip/recompute the `order` attribute on an incoming user tree before calling `MakeTreeBinary`, and must validate that `MakeTreeBinary`'s result is a well-formed tree (`nrow(edge) == 2*Nnode` is already checked — but check it *before* handing the object to `RootTree`, which is what actually dies); (2) TreeTools should not silently return a 47-edge non-tree for unrooted input. Orchestrator-reproduced deterministically twice; opus peer verification in flight at time of filing — a REFUTED verdict would realistically only revise the attribution detail (whether `MakeTreeBinary` truly emits a 47-edge object vs a measurement artifact), not the crash, which is an exit code. |
| T-346 | P2 | directed (warm-start pool) | **The pool is truncated by `targetHits`, not `maxReplicates`: the guard and all three docs cite the wrong bound, and the warning is additionally `verbosity`-gated** | `R/MaximizeParsimony.R:976-980` (guard + warning); docs `R/MaximizeParsimony.R:404`, `man/MaximizeParsimony.Rd:45`, `vignettes/tree-search.Rmd:322` | One root cause, three symptoms; found independently by the orchestrator and the opus finder, both **confirmed by running**. (a) Pool trees are consumed one per replicate *actually run*, and the search normally stops early on `targetHits` (default `max(10, nTip/5)`), not at `maxReplicates` (default 96). The literal vignette idiom `MaximizeParsimony(ds, tree = previousResult)` at full defaults on Vinther2008 supplies **52 MPTs, runs 10 replicates, silently discards 42 (81%)**, and emits NO warning because `52 > 96` is FALSE. Even when the warning does fire (52 trees, `maxReplicates = 40`) it names 40 when the true figure is 10. (b) The docs' "Trees beyond `maxReplicates` are not used" is literally true but reads as "trees up to `maxReplicates` *are* used", which is false whenever `targetHits` binds first — i.e. the common case — directly undercutting the NEWS/vignette motivation ("seeds the search with the topological diversity that tree fusing exploits"). (c) The warning is gated on `verbosity > 0L`, unlike the two ungated tip-drop warnings ~35 lines above at `:938`/`:944`; the Shiny app hardcodes `verbosity = 0L` (`inst/Parsimony/server/mod_search.R:630`), and the only test pins `verbosity = 1L` (`test-MaximizeParsimony-features.R:323`), so a regression flipping the gate is uncaught. House "asymmetric validation between sibling paths" pattern: silently dropping user-supplied *trees* should not be quieter than dropping user-supplied *taxa*. **Severity note:** haiku verifier confirmed REAL but argued P3 ("search finds the optimum anyway"); that test was on Vinther2008 where quality is saturated. Filed P2 because the house key names **search-quality** explicitly and the whole purpose of the feature is seeding fusion with topological diversity, which at defaults is delivered at 19% strength and silently. The finder separately **REFUTED** the adjacent worry that resuming inflates `hits_to_best` into a premature stop (Griswold1999, suboptimal 28-tree pool: resume 407 with 6/20 reps at best vs cold 407 with 1/20 — strictly better), so this is a docs/warning-accuracy and pool-utilisation issue, not a wrong-result one. Fix is a design decision (warn on actual consumption post-search, cap the pool, or document that `targetHits` bounds pool use) — NOT applied. |
| T-347 | P3 | directed (warm-start pool) | **`unpack_runtime` list path enforces `nrow` consistency but never `ncol` → segfault; the diff's own new pool-consistency contract is half-implemented** | `src/ts_rcpp.cpp:1633` (guards `se.nrow() != n_edge`) vs `:1639` (`flat[n_edge + i] = se(i, 1);` reads column 1 unconditionally) | **Confirmed by running** (opus finder): `TreeSearch:::ts_driven_search(..., startEdge = list(e1, e2[, 1, drop = FALSE]))` → Segmentation fault, exit 139. Honest characterisation: the OOB *class* is pre-existing (a bare n×1 matrix segfaults identically — same as T-323/T-328/T-344), but this diff **introduces a per-element consistency contract** whose own comment at `:1631-1632` says "Skipping a mismatched tree would shift every later replicate onto the wrong start, so an inconsistent pool is an error, not a filter" — and then implements only the `nrow` half. A caller who trusts the new contract still crashes. `:::`-only; `MaximizeParsimony()` cannot reach it (`lapply(startTrees, \`[[\`, "edge")` always yields n×2). Fix: add the symmetric `se.ncol() < 2` check beside the `nrow` one. |
| T-348 | P3 | directed (warm-start pool) | **New warning fires on the single-`phylo` path when `maxReplicates < 1`, contradicting the NEWS "unchanged in every respect" claim; message ungrammatical at n = 1** | `R/MaximizeParsimony.R:976-980` | **Confirmed by running** (orchestrator; haiku verifier REAL). `length(startTrees)` is 1 for a bare `phylo`, so `1 > maxReplicates` is TRUE whenever `maxReplicates < 1`. `MaximizeParsimony(ds, tree = <single phylo>, maxReplicates = 0L, verbosity = 1L)` emits "``tree`` supplies 1 starting trees but ``maxReplicates`` is 0; ...". Pre-diff no such warning existed anywhere, so this is a new user-visible emission on the single-tree path, contradicting `NEWS.md` bullet 1 ("passing a single `phylo` is unchanged in every respect"). Control at `maxReplicates = 1L` correctly emits nothing. Also "1 starting trees" has no singular/plural handling. Reachable because filed **T-341** (`maxReplicates < 1` unvalidated → silent fallback, score `-1` sentinel) leaves that state legal. Distinct fix from T-346 despite sharing the line. |
| T-349 | P3 | directed (warm-start pool) | **None of the three new test blocks can distinguish "pool consumed in rep order" from "tree 1 reused for every replicate"** | `tests/testthat/test-MaximizeParsimony-features.R:271-277`; `tests/testthat/test-ts-driven.R:282-285`; `tests/testthat/test-ts-start-tree.R:50-56` | **Confirmed by running** (opus finder + orchestrator). The features test asserts `expect_false(identical(cand(pooled), cand(single)))`, but its own `sameThrice` block immediately below *proves* the confound: measured `cand(pooled)=1468142`, `cand(single)=1429194`, `cand(sameThrice)=1683860`, so three copies of tree 1 also satisfy the assertion — i.e. the discriminating claim is satisfied by hypothetically buggy output. `test-ts-driven.R` only compares `bare` vs `listOfOne` plus `pooled$replicate_scores[[1]]`. `test-ts-start-tree.R:56` asserts `expect_equal(attr(pooled, "replicates"), 3L)`, which `maxReplicates = 3L` + `targetHits = 99L` forces regardless of pool consumption, and its score assertion is near-guaranteed when warm-starting from an already-optimal pool. **This is a test-strength defect only — the production code is CORRECT** (see the round log: serial shared-prefix discriminator and parallel `replicate_scores` discriminator both pass). **Working discriminator to adopt:** at the `ts_driven` layer, `Run(list(e1,e2))` vs `Run(list(e1,e3))` must differ (identical under the bug); measured 142585 / 110082 / 123717 (`e1,e1`) / 148788 (`e2,e1`). Also worth adding: a compressed-`multiPhylo` test, which the suite has none of. |
| T-350 | P3 | directed (warm-start pool) | **The diff's own `Resample()` doc fix leaves the same help page self-contradicting** | `R/Resample.R:146-149` (new `@param tree`) vs `R/Resample.R:182-185` (pre-existing `@details`) | **Confirmed by read** (orchestrator; haiku verifier REAL). The diff adds an authoritative and factually correct `@param tree` — "Each resample replicate searches from scratch; `tree` ... is not used as a starting point" (verified: `R/Resample.R:54` passes `startEdge = NULL`; `tree` is consumed only as a no-result fallback at `:128`, `:379`, `:399`). But the pre-existing `@details` at `:182-185` still says "Each resampling may begin from the optimal tree ... or a random tree". Both render on `?Resample`. Pre-diff the page was uniformly wrong (inherited MaximizeParsimony warm-start text); the diff makes the `@param` right and leaves the details wrong, so the page now contradicts itself on the one question a reader consults it for. |
| T-351 | P3 | directed (warm-start pool) / 8 (test suite health) | **Tier-1 CRAN test file runs 40.8 s against a documented < ~2 s budget; the diff adds 5.2 s** | `tests/testthat/test-MaximizeParsimony-features.R` (whole file); rule at `tests/testing-strategy.md:26-28`, `:100-102` | **Confirmed by running** (orchestrator; haiku verifier REAL). All `test-*.R` files WITHOUT a `ts-` prefix are Tier 1 — "< ~2 s per file", "no `skip_on_cran()`", run on CRAN. Confirmed the file has **zero** skip guards across 33 `test_that()` blocks; measured 40.8 s. Dominated by pre-existing `perturbStopFactor fires and sets perturb_stop attribute` (20.2 s). The diff's three new `multiPhylo` blocks add 5.2 s (4.08 + 0.59 + 0.49); the 4.08 s one alone is 2× the whole-file budget. **Framing: PRE-EXISTING severe breach, aggravated ~13% by this diff — not a regression it created.** (Note the sibling `test-ts-start-tree.R` is also Tier 1 and now runs 4.7 s, of which the new block is 0.72 s.) Fix is a policy call: guard the file, or move the slow blocks to Tier 2. |
| T-352 | P3 | directed (warm-start pool) | **`startEdge = list()` silently cold-starts while `startEdge = "foo"` errors — new asymmetry from the list path** | `src/ts_rcpp.cpp:1626` (`if (!mats.empty()) {`) | **Confirmed by running** (opus finder). An empty `VECSXP` leaves `start_n_edge = 0` and `start_edges` empty, so every replicate Wagner-starts: `Run(list())` gave `candidates_evaluated = 137858`, byte-identical to `Run(NULL)`; whereas `Run("not an edge matrix")` errors and `Run(list(NULL))` errors. House "asymmetric validation between sibling paths" pattern. `:::`-only — `MaximizeParsimony()` errors first at `R/MaximizeParsimony.R:915` (`stop("\`tree\` contains no trees.")`). |

<!--
Filing template (one row per verified finding):
| T-NNN | P1/P2/P3 | <area #> | **Title.** | `path:line` | Detail + fix + verifier verdict. |
-->
